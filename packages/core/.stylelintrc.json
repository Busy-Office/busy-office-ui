{
  "rules": {
    "selector-class-pattern": [
      "^eof-[a-z0-9]+(-[a-z0-9]+)*(__[a-z0-9]+(-[a-z0-9]+)*)?(--[a-z0-9]+(-[a-z0-9]+)*)?$",
      {
        "message": "Class names must follow .eof-block, .eof-block__part, .eof-block--variant (also allows htmx-* via ignore)",
        "severity": "error"
      }
    ],
    "custom-property-pattern": [
      "^eof-[a-z0-9]+(-[a-z0-9]+)*$",
      { "message": "Custom properties must be --eof-*" }
    ]
  },
  "overrides": [
    {
      "files": ["src/css/integrations/htmx.css"],
      "rules": { "selector-class-pattern": null }
    },
    {
      "files": ["src/css/reset/**/*.css"],
      "rules": { "selector-class-pattern": null }
    }
  ]
}
