# Objective grill — 348.1, 349.1, 350.1 (2026-09-24, local wake)

**Dispatched by rule 3** (`Objective 3 / 3 slices OVERDUE [348, 349, 350]`)
after rule 1 read 0 open P0.

**Scope.** All three labels are items closed in place today: `bc79e235`,
`03485ac9` and `661bc668`. Slice 351 grilled Slice 350's 2026-09-08 sweep, not
350.1's closure, so nothing was already covered. None of the three changed a
shipped artefact; they are loop scripts and text. That is the case 381.2 asks
about and has not decided, so rule 3 fired as written. The grill was kept
proportionate: three finders, each followed by an adversarial verifier told
to default to *does not survive*, and the thesis reading taken by hand.
Workflow `wf_e2bee258-01e`.

## Score

| subject | claims reproduced | findings | survived | refuted |
|---|---|---|---|---|
| 348.1's closure | 10 | 8 | 6 | 2 |
| 349.1's closure | 9 | 6 | 6 | 0 |
| 350.1's closure | 10 | 8 | 7 | 1 |
| **total** | **29** | **22** | **19** | **3** |

**What held.** Every headline figure reproduces, independently:
- **348.1:** 244 / 127 / 216 / 7, the 244/244 port agreement and its 238
  red-proof, and the new wording.
- **349.1:** 107 / 103 / 72 / 42 / 30 from the committed script.
- **350.1:** 147 / 21 / 61 / 136 / the 11, byte for byte.

**The defects are in the closures' own corrections.** Each of the three
closures corrected an earlier claim, and each correction carried an error of
its own. This is the pattern 346.1 measured, turning up in the next three
items, and its check could not see it: two of the three defects were outside
`ROADMAP.md`.

## 1. 348.1

- **F1 (medium, Evidence): "8 of 86 did not reproduce" is false.** Run over the
  86 revisions that existed when the check landed, with the recipe recorded in
  its header, it reproduces exactly: 8 of 86, distribution
  `{0: 78, 1: 3, 2: 3, 3: 2}`. All 8 were real closed ids. The closure measured
  a calendar-day window of 101 revisions, 15 of them written after landing.
  The old sentence was true of its data; the rate changed once the check ran.
  **Corrected** in LOOPS.md and in the DONE block.
- **F2 (medium, Evidence): 12 of 127, not 7.** At `5b8917e5`, `288.1` and `288.2`
  were real ids sitting as `###` headings, neither archived nor checkboxes. So
  12 revisions held a non-archived string (9.4%), and the code comment's
  "normally archived was true of the rest" was false. **Corrected.**
- **F3 (medium, Evidence): the hedge dropped the staleness instruction.** Every
  archived id in the ABSENT bucket is a CLOSED id. `175.4`, the check's founding
  incident, where the hand-off called a decided item open, sat in that bucket
  under both the old and the new wording. **Fixed:** the hedge says an archived
  id is closed, and to re-read any claim that it is open.
- **F4 (low, Evidence): the check's header still said "neither dead nor
  always-on"** in the present tense, beside LOOPS.md's new 216 of 244.
  **Dated**, with the post-landing figure added.
- **F6 (low, Evidence): the self-test cited as verification could not fail on
  the change.** Its code was byte-identical before and after. **Fixed:** the
  hedge is a named constant, a case asserts it (red-proved by a copy that
  restores "normally archived"), and another pins the `15.10` limit.
- **F8 (info): `record_iteration.py` shows this check's output only on a
  non-zero exit**, so an ABSENT list with no CLOSED id is never seen. **Noted**
  in LOOPS.md; no impact observed (every post-landing ABSENT revision also
  exited 1).
- **Refuted:** F5, "fired means two things" (each use has a local antecedent),
  and F7, a causal-clause hypothesis whose supporting count was wrong.

## 2. 349.1

- **F1 (medium, Evidence): "slices with work landed" still missed what the
  counter reads.** The counter credits the slice a row's label names and
  ignores the outcome. This grill's own arming set included a refused row
  (350.1). **Fixed:** rule 3 now says "named … whatever the row's outcome", and
  cross-refers 381.2 for whether shipped work should be required.
- **F2 (medium, Evidence): the replay used today's parser.** The counter as
  shipped at each dispatch reads 63 grills armed, 28 with three closed, 35
  delayed, about 27% open; 9 of the 72 were never counter-armed. **Restated**
  in LOOPS.md and the measure file.
- **F3 (low, Hypothesis): the delay had no size.** Every delayed grill fires
  within 1-21 further rows; Slice 377's fires 5 rows later. **Added.**
- **F4 (low, Evidence): the refusal re-cited a wrong reason.** The "sixth regex"
  `dispatch_status.py` refuses is a log-row pattern, and `roadmap_scope.py`
  already classifies slices by heading. Slice 349's heading, "nothing has ever
  compared the two", was also false: §6 step 0 had recorded the gap since
  `e721c20b`. **Corrected:** the refusal rests on the asymmetry alone, and the
  heading carries a note.
- **F5 (low, Evidence): "closed" wording survived** in the "Which loops close a
  slice" heading and `check-slice-refs.mjs`'s comment, and "24% of every arming
  set" was a pooled figure. **Fixed.**
- **F6 (info): provenance.** The run was at `f679f63a`, and the published output
  omitted lines. **Corrected** in the measure file.

## 3. 350.1

The refusal (rule 2 keeps counting Continue rounds) stands. The shortcut
written beside it did not.

- **F1 (medium, Evidence): lanes 1 and 3 read built pages.** Their inputs
  include `packages/core/src/` (JS writes inline styles at load),
  `packages/core/scripts/`, `apps/docs/scripts/` and `package-lock.json`, so an
  empty diff over the listed two could hide a moved input. **Fixed** in §3.
- **F2 (medium, Evidence): the diff ignored uncommitted changes**, which moved
  lane 3's total the same day (Slice 379). **Fixed:** the shortcut requires a
  clean tree.
- **F3 (medium, Evidence): "the last sweep's sha" was undefined.** The
  measurement opened a window at every Standardize row, but only 46 of 165 are
  sweeps. On sweep boundaries there are 39 windows, 8 and 18 of them
  input-free. **Fixed:** the base is the last row quoting all four lane figures.
- **F4 (medium, Evidence): the Accept's "cannot move" case went unanswered.**
  Slice 345's 0 → 52 had an instrument change, and the shortcut skipped the one
  comparison that would detect an environment move. **Fixed:** a skipped lane
  still runs, and its figure is compared with the base's; a difference is a
  finding.
- **F5, F6, F7 (low, Evidence).** The 136/147 is structural, since every item
  and hand-off edits `ROADMAP.md` or `RESUME.md`. The 11 empty windows hold no
  Continue row; they were not second rounds of one sweep. And the figures
  printed beside the rule were inputs-only while the test included the
  instrument. **Corrected.**
- **Refuted:** F8, a forecast with no defect in it.

## 4. The product thesis

| channel | reading | window |
|---|---|---|
| npm `@busy-office/ui` | 16, of which 5 are the current 0.8.0 | 2026-09-15..21, unchanged since Slice 381, so no new information |
| npm `@busy-office/create-ui` | 12 | same |
| jsDelivr | 29 | calendar month |
| GitHub stars / forks / discussions / open issues | 0 / 0 / 0 / 1 (the owner's) | all time |
| GitHub traffic | 20 views (6 uniques), 367 clones (132 uniques) | rolling 14 days |

**Framework code since Slice 381: 0 lines in 11 commits** (`git diff --numstat
60235d9c HEAD -- packages/core/src`). **What the channels cannot see:** copies
of `dist`, private mirrors, agent use, and busy-office-erp's vendored CSS.
**Comparators:** not re-read; SAP fundamental-styles remains the reference
point (Slice 377).

**The answer is unchanged, and the loop's own record sharpens it.** Two
consecutive grills (381, 382) found every subject shipped nothing a consumer
runs. The six P0 fixes still wait on 377.5. 381.2 is the item that asks
whether rule 3 should keep arming on such slices, and this grill is its second
data point.

## 5. Fed back

No new item. 381.1 (the correction check) and 381.2 (arming on non-shipping
slices) own the open questions. Every finding above was fixed or corrected in
the commit that lands this report.
