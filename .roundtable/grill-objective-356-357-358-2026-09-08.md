# Objective grill — Slices 356, 357, 358 (2026-09-08, cloud wake)

**Dispatched by rule 3** at `Objective 3 / 3 slices OVERDUE [330, 356, 357]`.
Rule 1 found no P0 (`grep -cE '^\s*[0-9]+\. \[ \].*P0' ROADMAP.md` → **0** across
**30** open items); rule 2 read `Standardize 1 / 4 ok`. Rules 4-8 not reached.

## Scope — the arming set names 330, and the material is 358

`dispatch_status.py` armed on `[330, 356, 357]`. **Slice 330 is itself an
Objective grill** (of 310, 328, 329) and no Continue row built it; what armed the
counter is Slice 358's row, which names the *item* `330.1` that grill filed. So
the material actually available is **356, 357 and 358**, and 358 is the slice
under grill wherever the counter says 330. Checked against the grill history —
none of 356, 357, 358 has been covered before:

```
grep -hoE '^## Slice [0-9]+ — Objective grill of Slices? [0-9, -]+' ROADMAP.md ROADMAP-archive.md
  # newest four: 341 (316,319,339) · 346 (322,342) · 351 (324,325,347,350) · 355 (353,354)
```

`.roundtable/INDEX.md` reports **203 files, 147 cited, 4 repeated subject(s)** —
read before grilling, per the playbook's step 0.

## Verdict — 60 of 64 published assertions reproduce

**The counting rule, stated so the number is re-countable:** one assertion = one
row of the three tables below. Several rows carry more than one figure — lane 2's
`74 / 242 / 230 / 8` is one row and four numbers — so a figure-level count would
be higher and would depend on how the grouped rows are split. Count the rows.

| slice | assertions | reproduce | fail |
|---|---|---|---|
| 358 — the sampling census | 26 | 25 | 1 |
| 357 — Standardize sweep, per-lane base rate | 27 | 26 | 1 |
| 356 — the Demo-signal count | 11 | 9 | 2 |
| **total** | **64** | **60** | **4** |

**Every headline figure — the one each slice red-proved — reproduces to the
digit.** All four failures are in a sentence that *characterises* a measurement
rather than in the measurement: a composition summary that drops two verdicts, an
attribution to the wrong item, and a per-page range with no instrument recorded.
That is **192.1's shape** — *the defect lands in what shipped BESIDE the number* —
arriving three slices in a row, in the window where Slice 354 measured 192.1 as a
name for a defect rather than a step anyone executes.

---

## Slice 358 — the sampling census: 25 of 26

Re-run against `git show ada0f384:ROADMAP.md`, the revision every figure names.

| assertion | published | re-run |
|---|---|---|
| live file, lines | 11,809 | **11,809** ✓ |
| figure-carrying lines | 4,000 | **4,000** ✓ |
| numeric tokens | 8,573 | **8,573** ✓ |
| candidates (published filter, verbatim) | 153 | **153** ✓ |
| FP — `mean` the English verb | 7 | **7** ✓ |
| FP — "sample" = a code sample | 7 | **7** ✓ |
| FP — "sample" = a `loop-metrics.jsonl` datum | 7 | **7** ✓ |
| FP — other | 36 | **36** ✓ |
| CENSUS | 64 | **64** ✓ |
| SAMPLE | 13 | **13** ✓ |
| SAMPLE-timing | 15 | **15** ✓ |
| EXTRAP | 4 | **4** ✓ |
| verdict rows total | 153 | **153** ✓ |
| rest on a sample or extrapolation | 32 | **32** ✓ |
| instrument noise | 57 (37.3%) | **57 (37.25%)** ✓ |
| candidates with no verdict | 0 | **0** ✓ |
| verdicts for non-candidates | 0 | **0** ✓ |
| base rate | 3.8% | **153/4,000 = 3.83%** ✓ |
| open items at `ada0f384` | 31 | **31** ✓ |
| open-set body lines | 1,155 | **1,155** ✓ |
| Slice 301's archived body | 82 lines | **82** ✓ |
| Slice 301's live pointer | 4 lines | **4** ✓ |
| widening the marker set | 134 → 153 | **134 → 153** ✓ |
| …of which real sample lines | +5 | **+5** ✓ |
| …of which false positives | +12 | **+12** ✓ |
| **…and the composition accounts for the move** | **implied** | **✗ — see below** |

The verdict table was reconciled by **set equality**, not by addition: the 153
line numbers in `measure-330.1-sampling-census-2026-09-08.md` are exactly the 153
lines the published filter selects — 0 unverdicted, 0 verdicts for a
non-candidate. That is the strongest reproduction in this grill.

### The one failure — the widening delta drops two verdicts, and 134 + 5 + 12 = 151

The slice says the widened marker set *"moved 134 → 153, adding **5** real sample
lines against **12** more false positives"*. The delta is **19**, and the report's
own per-line verdicts on those 19 lines are:

| verdict on the 19 new lines | n |
|---|---|
| `SAMPLE-timing` | 5 |
| `FP-other` | 12 |
| **`CENSUS`** | **2** |

Lines **6052** (*"2 failures in the last 40 runs on `main`"*) and **6404**
(*"8 of 20 run zero cases"*) are both verdicted `CENSUS` in the report and are
absent from the sentence. A reader reconciling it gets 151 against a stated 153.

**The census is not affected.** The 32/57/64 verdicts, the base rate and the
open-set reading are all taken over the full 153 and all reproduce; what is wrong
is one summarising sentence about how the filter got there. Corrected in place.

---

## Slice 357 — the sweep and the per-lane base rate: 26 of 27

**The headline command was re-run at the revision it names**, transcribed from the
slice and extended to run both window forms in one pass:

```
python3 <scratch>/baserate.py 30d94cc7
  30d94cc7 caret 139 windows, span 1869 {neither 20, core_only 7, docs_only 38, both 74} sum 139
           | neither 14.4% | neither+docs_only 58 41.7%
  30d94cc7 plain 139 windows, span 2008 {neither 15, core_only 7, docs_only 38, both 79} sum 139
```

Every bucket, both spans and both rates are the published values. The four
buckets partition (20 + 7 + 38 + 74 = 139, asserted in the script), and the
`a..b` control returns **139 / 15 / span 2008** as published — its `no-both` 13 is
350.1's third quantity, which this variant does not compute and which is
therefore **not** re-derived here.

| lane / claim | published | re-run |
|---|---|---|
| base rate — windows | 139 | **139** ✓ |
| base rate — span, `a..b^` | 1,869 | **1,869** ✓ |
| base rate — buckets | 20 / 7 / 38 / 74 | **20 / 7 / 38 / 74** ✓ |
| base rate — buckets partition | = 139 | **= 139**, asserted in the script ✓ |
| base rate — lanes 1 and 3 blind | 20 of 139 = 14.4% | **14.4%** ✓ |
| base rate — lane 2 blind | 58 of 139 = 41.7% | **41.7%** ✓ |
| base rate — `a..b` control | 139 / 15 / span 2,008 | **139 / 15 / 2,008** ✓ |
| lane 1 — dead style attributes / pages | 0 / 0 | **0 / 0** ✓ |
| lane 1 — live inline attributes | 1,365 | **1,365** ✓ |
| lane 1 — declarations inside them | 1,813 | **1,813** ✓ |
| lane 1 — multi-declaration attributes | 345 | **345** ✓ |
| lane 1 — dead declarations / pages | 11 on 9 | **11 on 9** ✓ |
| lane 1 — the 9-page refusal set, page for page | (listed) | **identical** ✓ |
| lane 2 — files / rules / distinct / repeating | 74 / 242 / 230 / 8 | **74 / 242 / 230 / 8** ✓ |
| lane 2 — the new closing line names its tree | derived | **prints `packages/core/src/css`** ✓ |
| lane 2 — core CSS last touched | `e4742fd4`, Slice 338, 2026-09-07 | **`e4742fd4`, Slice 338, 2026-09-07** ✓ |
| lane 2 — commits since, first-parent | 47 | **47 at `30d94cc7`** ✓ (48 at `919d55d5`) |
| lane 3 — documentation pages | 119 of 128 | **119 of 128** ✓ |
| lane 3 — median / mean / total | 798 / 959 / 114,124 | **798 / 959 / 114,124** ✓ |
| lane 3 — `/concepts/which-pattern/` | 310a + 2,015g | **310a + 2,015g** ✓ |
| lane 4 — dispatch region | 7,548 words, 41.8% | **7,548, 41.8%** ✓ |
| lane 4 — `### Step 0c` body words | 1,516, +194 | **1,516, +194** ✓ |
| lane 4 — sections moved | 1 of 16 | **1 of 16** ✓ |
| lane 4 — the block's own assertion | 7,492 + 56 = 7,548 | **holds** ✓ |
| archive share at `30d94cc7` | 5,307 / 11,564 = 45.9% | **5,307 / 11,564 = 45.9%** ✓ |
| archive share at `919d55d5` | 5,525 / 11,802 = 46.8% | **5,525 / 11,802 = 46.8%** ✓ |
| **lane 2's blindness vs "the rate `350.1` records"** | **nearly 3x** | **✗ — see below** |

### The one failure — the multiple is right and the item it is attributed to is wrong

The slice reads: *"Lane 2 is blind on `neither + docs_only` = 58 of 139 (41.7%) —
nearly **3x** the rate `350.1` records"*.

- **`350.1` records 10.1%** (14 of 138 windows). 41.7 / 10.1 = **4.1x**.
- **351.1 amended that to 14.4%**, on the `a..b^` window. 41.7 / 14.4 = **2.9x**,
  which is the "nearly 3x" in the sentence.

So the multiple is arithmetically right against **351.1's** figure and is credited
to **350.1** — in the bullet immediately after the one that says *"which
reproduces `351.1`'s amended figure to the window"*. The slice is inconsistent
with itself one bullet apart about which item owns 14.4%.

**Nothing downstream moves.** The finding — that lane 2's input set is not lanes 1
and 3's, and that the OR credits it with 38 `docs_only` windows it cannot see — is
unaffected under either base: 41.7% against 10.1% is a *larger* gap, not a
smaller one. Corrected in place, with both multiples stated.

---

## Slice 356 — the Demo-signal count: 9 of 11

| assertion | published | re-run |
|---|---|---|
| committed script files | 160 | **160** ✓ (and 160 at `78e96150`) |
| `apps/docs/scripts` / `packages/core/scripts` / `scripts` / `examples` | 78 / 32 / 12 / 38 | **78 / 32 / 12 / 38** ✓ |
| carry a Demo-family signal, non-comment | 8 of 160 | **8** ✓, and the **same 8 files** |
| gate message — pages swept | 24 | **24** ✓ |
| gate message — carry a paired preview | 3 | **3** ✓ |
| gate message — exempt | 21 | **21** ✓ |
| the three preview pages, and their counts | scale 1, first-screen 3, htmx 1 | **1 / 3 / 1** ✓ |
| exempt pages rendering `bo-*` outside preview and `<pre>` | 21 of 21 | **21 of 21** ✓ |
| the refused predicate's base rate | 24 of 24 | **24 of 24** ✓ |
| **per-page range, low end** | **`other=1`** (container-queries, versioning) | **✗ — 4 and 5** |
| **per-page range, high end** | **`other=99`** (js-behaviors) | **✗ — 103** |

The 3 / 21 / 24 split reproduces from **two independent instruments** — the gate's
own HTML-string reading, and a browser probe written this wake against
`browser-harness.mjs` + `serve-dist.mjs` at 1440px, `networkidle0` + 2 rAF.

**The probe was red-proved before its 24-of-24 was quoted**, because a 100% reading
is a defect until shown otherwise. Two synthetic pages were written into `dist`,
each asserted to carry what it was supposed to carry before the probe ran, and
both deleted afterwards:

| control | contains | probe reads |
|---|---|---|
| negative | `bo-*` markup **only inside `<pre>`** | **other = 0** |
| positive | one visible `bo-*` outside any `<pre>` | **other = 1** |
| a real page (`/concepts/container-queries/`) | — | other = 4 |

So the probe can report zero, and the 24-of-24 is a property of the tree rather
than of the detector. Slice 356's refusal of the widened predicate stands.

### The two failures — a per-page range no command can re-derive

*"ranging from `other=1` (`/concepts/container-queries/`,
`/getting-started/versioning/`) to `other=99` (`/concepts/js-behaviors/`)"*
appears **only in `ROADMAP.md` prose**:

```
grep -rn 'other=99' --include='*.md' --include='*.mjs' .   # ROADMAP.md:796 — and nothing else
ls .roundtable/ | grep -iE '356|328'                       # no report file
```

The probe that produced it was a scratchpad file, is gone, and no command was
recorded beside the claim. Re-measuring with the nearest reasonable reading of the
same words — every visible element carrying a `bo-*` class inside `.docs-content`,
excluding anything inside `<pre>`, inside a `.demo-pair__preview`, or inside shell
chrome — gives **4**, **5** and **103** for the three named pages.

**What reproduces is the ordinal, not the value.** Those same two pages are the
minimum of the 24 and that same page is the maximum under both instruments, so the
sentence's *shape* is right. What cannot be checked is its numbers, and the two
instruments disagree by **4x** at the low end. This is not a claim that 1 and 99
were wrong when taken — it is that nothing recorded makes them checkable, which is
CLAUDE.md's *write the command next to the claim* in the same window that
`353.2` filed the identical complaint about a metric.

Amended in place: the range is now attributed to its unrecorded probe, with this
wake's re-measurement and its exclusion rule stated beside it.

---

## What is NOT proposed, and why

**No new item, no gate, no instrument.** All four defects are one sentence each
and are corrected in place (236.2 permits amending; all three slices are live in
`ROADMAP.md`).

The tempting move is a fifth loop-mechanics item — *"a characterising sentence
beside a measured number is unverified prose"*. It is refused on this repo's own
precedent, not on taste: **355.3 refused exactly that** ten slices ago
("a sixth item restating a shape with no mechanism behind it is what this repo
refuses"), `341.1`, `349.1`, `350.1`, `351.1` and `353.2` are already open on loop
mechanics, and Slice 354 measured that 192.1 — the rule that already covers this —
is cited as a name **12 of 21** times and applied **1 of 161** slices. A sixth
statement of a rule with a 1-in-161 execution rate does not raise the rate.

**And no gate is possible here anyway.** *"This sentence characterises the
measurement correctly"* is semantic — roadmap 94.11's wall. The checkable shape
(*a summary sentence whose numbers sum to the delta*) fires on correct sentences
too: two of the three defects are not arithmetic at all.

## Not re-derived, said plainly

- Slice 358's **hand verdicts** on the 153 candidate lines were re-counted and
  reconciled by set equality; the *judgement* on each line was not re-made. Two
  spot-reads agreed (the `CENSUS` verdicts on 6052 and 6404 are what the failure
  above rests on).
- Slice 357's **injection red-proofs** (the `list-style/margin/padding` body into
  each tree) were not re-run; the baseline they moved from — `242 / 8 / x3` — is
  re-measured above and matches.
- Slice 356's **injection red-proof** of the gate (3 → 4 judged) was not re-run;
  the 3 / 21 split it returns to is re-measured above by two instruments.
- `350.1`'s **`no-both` = 13** is not computed by this grill's variant of the
  base-rate script.

## Environment

Cloud wake. Trap 1 bit: `git branch --show-current` read **EMPTY**, fixed with
`git fetch origin main && git checkout -B main origin/main` before any commit.
Trap 2: shallow at 50 commits, `--unshallow` clean in one pass — **2,085**
commits, no `shallow.lock`, tags again arrived with it (`git tag | wc -l` → **8**,
run rather than assumed). **It was load-bearing, and not for the reason a wake
first reaches for**: the three revisions every figure names sit **4, 3 and 2**
first-parent commits behind `HEAD` and a shallow clone reaches all of them. What
a 50-commit clone cannot support is the base-rate script, whose 139 windows span
**1,869** commits (2,008 on the `a..b` form) — it would have silently classified
a truncated history rather than failing.

**No screenshots — a cloud wake has no Podman**, and this grill owes none: its
diff is `ROADMAP.md`, this file and the recorder's own outputs.
