# Objective grill — Slices 186, 189, 190, 191 (2026-08-29, cloud wake) — SECOND OF TWO

**Two dispatchers ran this same rule-3 grill concurrently and both are kept**,
per Step 0c. The other pushed first as **Slice 192**, with
`.roundtable/grill-objective-186-189-190-191-2026-08-29.md`; this one ships as
**Slice 193** under a `-b` filename because the window, and therefore the
conventional filename, is identical. **The two reports share no finding.** 192
reads the window for *how each claim was established* (asserted vs measured in
173.2, and 190.2's own instrument defects); this one reads it for *what the
window left behind* (the arming ratio, an unexecuted reopen condition, and the
corpus 186 measured), plus a control pass re-measuring the window's artefact
claims. Where they touch, **192 has priority and is credited** — see §D.2.

**Dispatcher rule 3.** `dispatch_status.py` read
`Objective 4 / 3 slices since 2026-08-29 03:48 OVERDUE [186, 189, 190, 191]`.
Rule 1 found no open P0 (`grep -nE 'P0' ROADMAP.md` returns closed slice headings
and 190.1's explicit *"Not a P0"*) and GitHub intake **0 open issues**, asked via
the API. Rule 2 read `Standardize 2 / 4 ok`. No new input, so Step 1 triaged
nothing.

`.roundtable/INDEX.md` checked first: no prior grill covers this window.

**TWO COLLISIONS, both caught by Step 0c's working half before a single
commit.** The mandated `git fetch origin main` immediately before this wake's
first commit saw `44611ba1..a9470314` — the other dispatcher had taken **rule 4**
and closed **190.2** (`7152cfa2`). Not a duplicate dispatch: different rule,
different item. This grill fast-forwarded and **re-ran every figure on
`a9470314`**; the two that moved are named where they appear (`check:claims`
141 -> **144**, `ROADMAP.md`'s ratchet 26 -> **27 up**).

The second fetch, taken immediately before committing the re-run, saw
`a9470314..44f8bb37` — and **that one IS a duplicate dispatch**: the same rule-3
grill of the same four slices, filed as Slice 192, with the same report
filename. It pushed first. This work was rebased onto it and renumbered
**192 -> 193** rather than landing on a stale base, which is Slice 190's
precedent applied a second time. **This is the fourth recorded collision and the
second caught before any commit** — and the first where both dispatchers chose
the same output filename, which is why the rename is part of the record rather
than a detail. Findings A and C were re-run in full on `44f8bb37` and are
unchanged: still `32 crossed · 10 grill-dependent`, still the same arming
set, still **42 of 558**.

**`ENVIRONMENT.md` trap 1 bit, exactly as written.** The fast-forward left
`git branch --show-current` **empty** — this container started detached — and
that was found by running the check the file mandates before the first commit,
not at push rejection. Repaired with `git checkout -B main origin/main`. The
file's own correction reproduced too: `git rev-parse --short main HEAD` returns
`fatal: Needed a single revision` on a container where `main` demonstrably
exists.

**Cross-cut.** Three of the four slices under grill are themselves Objective
grills, and the one durable obligation any of them left behind — a reopen
condition written by 167.1 — **fired six hours and forty-nine minutes later and
has never been executed**, through five subsequent grills. The window's
*artefact* claims all re-measure correctly (§D, 3 of 3); what fails is the
loop's ability to re-read a condition it wrote down. **Hypothesis**, n = 4
slices, one corpus.

---

## A. The Objective counter is at its most self-armed reading on record — **Evidence** (10 of 32 dispatches; this one 3 of 4)

Rule 3's own text invites this re-run: *"The command is in
`.roundtable/grill-objective-164-167-169-2026-08-28.md` §B; re-run it, the
figures are snapshots."* Re-run verbatim at `44611ba1`:

```
python3 - <<'PY'   # §B of grill-objective-164-167-169, unmodified
...
PY
# 32 dispatches crossed · 10 depended on grill follow-up      (1112 rows)
```

| | filed 2026-08-28 | at HEAD |
|---|---|---|
| rule-3 dispatches that crossed 3 | 26 | **32** |
| of those, would not have crossed without a grill-derived slice | 7 (27%) | **10 (31%)** |

**This wake's arming set is the extreme case and is a first.** The previous
maximum grill-derived share in any arming set was 2 of 3 (`[164, 167, 169]`).

```
2026-08-28 21:44   ['173','176','177','178']   grill-derived 0/4
2026-08-29 01:46   ['180','183','184']         grill-derived 0/3
2026-08-29 11:29   ['173','185','186','187']   grill-derived 1/4  ['186']
2026-08-29 14:41   ['186','189','190','191']   grill-derived 3/4  ['186','189','190']
```

Excluding grill-derived slices this counter reads **1 of 4** and rule 3 would
not fire.

**This is NOT the retired product-vs-machinery ratio**, which Slice 192 checked
for and correctly declined to raise. That one asks what the loop's output is
*about*; this asks what arms **rule 3**, is a different measured quantity, and
is re-run on the standing invitation in rule 3's own paragraph.

**The instrument, before its output is believed.**

- **Reconciled against two independent counts.** `ds.rows()` returns **1115**
  against a raw `grep -c '^- '` of **1115**; the arming set is
  character-identical to `dispatch_status.py`'s own printed
  `[186, 189, 190, 191]`, which is a different code path from the one this
  script walks. The set was read off the *pending* dispatch before the second
  collision and off the *landed* `2026-08-29 14:41` row after it — the same
  four slices either way.
- **Red-proved by injection, and it discriminates.** Emptying `G` gives
  `(32, 0)`; removing `'41'` from `G` gives `(32, 9)` — exactly one fewer,
  the `2026-08-19 05:57` row flipping to non-dependent. Injecting `'191'`
  into `G` correctly leaves `(32, 10)`: this wake's set is already dependent at
  1 of 4, so making it 0 of 4 changes no verdict. A detector that moved there
  would be the broken one.
- **The file-order bias the previous grill flagged is now inert, measured
  rather than assumed.** That grill warned that `if m.group(1) not in head`
  makes the first heading win, and that for slice 172 this biased the result
  *toward* the criticism. Reading `ROADMAP-archive.md` first instead of
  `ROADMAP.md` first gives an identical `G` (set difference **empty**), an
  identical `(32, 10)`, and an identical arming set: the archive sweep leaves a
  stub whose heading text is byte-identical to the archived section's.
  `check:repo` agrees from the other side — *"173 slice number(s) each heading
  one section"*.

**Counter-evidence, and it decides the action: excluding grills would defer
grill-filed work — and this wake watched that happen.** Had rule 3 not fired,
rule 4 would run, and its oldest *dispatchable* items were **190.2** and
**190.1**, both filed by Slice 190, a grill. **The other dispatcher took rule 4
concurrently and closed 190.2 while this grill was running** — grill-filed work
landing, in the same hour, on the same queue. Of the five open items at
`a9470314`, three are owner-blocked (`15.12`, `112.3`, `112.4`) and of the two
that remain, **one (`190.1`) was filed by a grill**. The grill lane is not
decorating the backlog; at HEAD it is most of it.

The same shift shows in what the backlog is made of:

| | slices that are grills | items filed under a grill slice |
|---|---|---|
| whole corpus | 24 / 142 | 66 / 558 (12%) |
| slices 1-159 | 14 / 114 | 42 / 491 (9%) |
| slices 160+ | 10 / 28 | 24 / 67 (**36%**) |

The 160 boundary is a choice of mine, not a natural one, and is stated as such;
the 180+ cut reads 6 / 16 (38%), so the figure is not an artefact of where the
line is drawn. Item count reconciled: 558 parsed against
`grep -cE '^\s*[0-9]+\. \[[ x]\]'` returning 43 + 515 = **558**.

**So: recorded, not gated — again.** Slice 175 already refused a heading
classifier inside the dispatcher ("the sixth regex"), and nothing here is new
evidence against that refusal. What is new is the extreme point, and rule 3's
own paragraph is the right place for it to be re-measurable from.

---

## B. 167.1 wrote a reopen condition. It fired the same day and has never been executed — **Evidence**

167.1's verdict on `CLAUDE.md` (2026-08-28, now in `ROADMAP-archive.md:20625`):

> **Watch, not a finding:** 7 of its 16 `##` sections — **1,893 of 4,600 section
> words, 41%** — are all on one subject, whether a detector can fail. They are
> not duplicates (injection validity, base rate, heuristic self-test, CI-only,
> structural assertion, first output, reported number are seven distinct traps),
> but a wake reading one gets no pointer to the other six. **Reopen if an eighth
> is added without folding.**

**The seven are identifiable to the word, which is what makes this checkable.**
Splitting `CLAUDE.md` on `^## ` at 167.1's own closing commit `e3844c49`:

```
                            sections  section words  file words  the seven
e3844c49  (close(167.1))       16          4,570        4,759      1,893
e3844c49^ (its parent)         16          4,570        4,759      1,893
0131ebc5  (slice 172's grill)  17          4,860        5,062      1,893 + 290
HEAD      (44611ba1)           17          5,046        5,248      1,893 + 290
```

**1,893 reproduces exactly.** (My splitter reads 4,570 section words where 167.1
read 4,600 — a 30-word difference in how heading lines are attributed, the same
discrepancy 167.1 and 191.2 both record for their own splitters. It does not
touch the seven, the section count, or the 4,759.)

**The eighth was added by the very next Objective grill, as a new `##`, not
folded.** `0131ebc5` — *"docs(roundtable): Objective grill 169/170/172 — a green
red-proof is no evidence"* — added **"A green red-proof is a defect in the
INJECTION until proven otherwise"**, 290 words. Ordering, in UTC because the two
commits carry different offsets and the naive stamps read backwards:

```
e3844c49  close(167.1)                 2026-08-28T06:47:08Z
0131ebc5  the eighth section           2026-08-28T13:36:48Z   (+08:00 -> 21:36 local)
                                       ------- 6h 49m -------
```

At HEAD the subject holds **8 of 17 sections and 2,183 of 5,046 section words
(43%)**, up from 7 of 16 and 41%.

**Nothing has executed the watch.** `grep -rn 'without folding'` across
`ROADMAP.md`, `ROADMAP-archive.md`, `.roundtable/*.md`, `LOOPS.md` and
`CLAUDE.md` returns **exactly one line — 167.1's own**. Five Objective grills
have run since (**175, 179, 186, 189, 190**) and none names it. So has the
whole of Slices 173-191.

**The eighth section is the one case in the file that argues for its own fold**,
which is why the watch was worth writing. Its opening sentence:

> The rule above it — *verify the injection, not just the red result* — is
> written, worked through five examples, and was violated **five times in two
> slices** by an agent that had read it.

It is a restatement of section six in a different grammar, added because the
first form was not being acted on. That is the strongest fold candidate in
`CLAUDE.md`, and also the strongest argument that folding would lose something.
**Deciding it is not this wake's job** — it is a judgement about the doctrine
file every wake reads, and the Objective playbook's exit is a report and a
triage, not a build. Filed as 192.1 with the decision framed and both directions
made satisfying.

**Why `report_loop_prose.py` could not have caught it.** Its `ratchet` block
reads `CLAUDE.md  28 up  never cut` at HEAD — correct, and it is the signature
§3 asks about. But "eight sections on one subject" is not a word count; it is a
judgement about what prose MEANS, which is 94.11's line. **No gate is proposed.**

---

## C. 186's "1 of 275" measured the Accept-criterion corpus, not the loop's obligations — **Evidence** (42 of 558)

186 concluded that recurring obligations are vanishingly rare — *"none of that
shape exist. n = 1, out of 275"* — and robustness-checked it by **widening the
needle set from 11 phrases to 21, which added zero hits**: *"the phrase space is
saturated, not under-sampled."*

The needle set was not the binding limit. **The corpus was.** 186 parsed only
the Accept block — *"criterion bounded at the first blank line OR the next
`N. [x]` item marker"* — and 167.1's watch sits roughly eighty lines below its
Accept, in the item's body. No needle width could have reached it.

Parsing the **body** of every item in both files (bound at the next item marker
or the next `## ` heading, with the Accept block 186 read removed):

```
items parsed: 558   (raw grep total: 558)
items whose body outside the Accept block carries
  reopen / re-open / re-raise / revisit:            42
```

Reconciled: 558 parsed = 558 raw, so the walk sees every item.

**The population is not uniformly rotting, and that is the load-bearing half.**
Hand-read, the 42 split into two kinds:

- **Executed.** The css-repeats *"joined-control x4 — reopen trigger is a THIRD
  component"* condition has been re-checked and reported **four times** —
  slices 166, 169, 178 and 191 — because `LOOPS.md` §3's Standardize playbook
  names the report that carries it. Slice 29's *"revisit if the next window
  pushes past ~30:1"* fired and was acted on.
- **Unread.** 167.1's, and the large class of *"re-open only if a real screen
  needs it"* refusals (slices 117, 119, 120, 121, 123, 125, 136, ...), whose
  trigger is an event no instrument watches.

**So the predictor of whether a reopen condition is ever executed is whether a
playbook step names the thing that carries it — not whether the condition is
checkable.** 167.1's is mechanically checkable in one command and was never
run; the css-repeats one is equally checkable and has been run four times. n = 2
on each side, so this is stated as **Hypothesis**, not Evidence.

**Refused here, deliberately: a "reopen-condition register" gate or report.**
The phrase is a checkable *shape*; whether the trigger has fired is semantic for
most of the 42 ("a real screen needs it"), and a register that lists 42 rows
nobody can adjudicate is 94.11's ceremony with extra steps. What is proposed
instead (192.2) is one measurement and a decision, with refusing named as a
satisfying outcome.

---

## D. Controls — the window's artefact claims, re-measured

Three of three hold. This is the half that keeps the cross-cut honest: the
slices under grill measured their mechanisms correctly.

**1. 191.1's `ratchet` block, at HEAD.** Its predictions held exactly, including
one written into the hand-off before the fact:

| | filed at `aa7512f5` | at HEAD | expected |
|---|---|---|---|
| `CLAUDE.md` | 27 up, never cut | **28 up, never cut** | +1 — 189.1 added a paragraph |
| `LOOPS.md` | 18 up, last cut `3006da0a` | **19 up, same cut** | +1 — 191's own §3 edit |
| `DESIGN.md` | 20 up, never cut | **20 up, never cut** | unchanged — nothing touched it |
| `ROADMAP.md` | 23 up, last cut `2ae54a4a` | **27 up, same cut** | +4 commits |

**2. 190.1's blast-radius walk reproduces — its denominator does not.** Walking
the built site in a real browser and querying `.bo-data-table
.bo-form-field__message`:

```
pages walked: 127
pages nesting the message inside a data table: 1   -> /patterns/editable-grid/
RED-PROOF on / : before=0 -> injected=true, count=1   (the query is not dead)
```

**1, exactly as filed**, and the weaker page-level instrument reproduces exactly
too (`grep -rl` over `dist` → **9**, against 190.1's "9 of 138"). But **no
enumeration of the built site returns 138**: `distPages()` gives **127**,
`distPages(..., {skipRedirects:false})` gives **137**, raw `find -name '*.html'`
gives **165**. Nothing between 190's commit and HEAD adds or removes a page
(`cfb5352` added a *script*, not a page). The report printed the walk's output
without its invocation, so it can only be re-derived, not re-run — which is
CLAUDE.md's *"write the command next to the claim"* with a one-unit cost rather
than a wrong conclusion. **The finding is unaffected; the denominator is
unreconciled by one.** Slice 192's report quotes *"1 of 138"* from the same
source, so the correction applies to both — **192 stated it first and is
credited**; this only re-derives the denominator it inherited.

**3. 190.3's specificity comment is correct — 2 of 2 — and it cost this grill
two dead instruments to confirm.** Racing each selector against a reference
ladder placed later in the sheet:

```
data-table's rule   beats (0,3,1), loses to (0,4,0)  -> (0,4,0)   comment says (0,4,0)  OK
form-field's :is()  beats (0,3,0), loses to (0,3,1)  -> (0,3,1)   comment says (0,3,1)  OK
```

Both dead instruments are worth recording, because both are this file's own
documented traps arriving in a fresh probe:

- **First attempt: references that did not match the node.** The ladder was
  built from ancestor chains (`.bo-data-table .bo-form-field .bo-form-field
  .bo-form-field__message`). A reference that matches nothing never applies, so
  the tested selector "wins" every rung — the probe reported the data-table rule
  beating **(0,5,0)**, which is impossible. Fixed by asserting
  `host.matches(ref)` for every rung before racing, and by building the ladder
  from a repeated class (`.m.m.m` is (0,3,0) and matches by construction). The
  ladder now self-checks: a known (0,3,0) beats (0,2,0) and (0,2,1) and loses
  from (0,3,0) up.
- **Second attempt: racing a paraphrase instead of the artefact.** The selector
  was typed from ROADMAP 190.3's quotation, which elides the `:has()` arm as
  `…:user-invalid))`. Raced literally that is **(0,3,0)** — one step below the
  shipped rule — and the probe "found" the comment wrong. The elided tokens are
  exactly what carries the extra element: the built CSS reads
  `:has(input:user-invalid,select:user-invalid,textarea:user-invalid)`, and
  `:has()` takes its most specific argument, so `input:user-invalid` is (0,1,1)
  and the whole rule is (0,3,1). Settled by reading the selector out of
  `apps/docs/dist/_astro/*.css` — the artefact — rather than out of prose about
  it.

The ellipsis is marked, so 190.3 states nothing false; it elides the tokens its
own number turns on. One line, no item filed: the load-bearing artefact (the CSS
comment) is correct and carries its method.

---

## Gates, and what this wake could not do

Green at `44611ba1` before the collision — core `build` + `test`, `docs:build`,
`check:repo`, `check:claims` **141**, `check:layout` **127** pages, `test:axe`
**127 pages x 2 widths, zero violations** — and the three that the merge could
move were re-run on `a9470314`:

```
check:claims  144 documented behaviours verified live      (141 + 190.2's three)
check:repo    exit 0 · selftests 45 gates, 16 heuristic all self-tested
              slice-refs 389 citation(s), 173 slice number(s)
              vendor-names 543 file(s), all roots clean
```

`check:claims` moving **141 → 144** is an independent confirmation of the other
dispatcher's item: 190.2's own Accept required that *"the claim count moves by
what was added"*, and three sentences were added.

**This was a cloud wake: no Podman, no `localhost:8081`, no screenshots at
1440px and 390px in light and dark.** Nothing here needed one — the diff is
markdown only, and every measurement above is a DOM, computed-style or history
assertion, which `ENVIRONMENT.md`'s second list says a cloud wake takes. That is
an argument from the diff and from the method, **not a visual check, and it is
not claimed as one.** No `verifier` agent is available in this session, so the
staged diff was read by hand instead — said plainly rather than logged as a
verifier pass.
