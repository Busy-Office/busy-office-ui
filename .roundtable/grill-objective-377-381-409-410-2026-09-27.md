# Objective grill — 377.14 and 377.16 in full; 377.13, 377.15, 377.17, 381.1, Slices 409 and 410 narrowed (2026-09-27)

Dispatched by rule 3, `Objective 4 / 3 OVERDUE [377, 381, 409, 410]`, after
rule 1 read 0 open P0 and rule 2 read `0 / 4`. Workflow `wf_07f2da2a-8bb`
ran 3 hunts, 3 refute-first verifiers and a completeness critic against the
live container at `23a22cce` (`/build-id.json`: dirty false).

## 0. Scope — every label resolved, and what shipped

The armed labels are item ids, not slices (LOOPS §6 step 0). Label 377 is
377.13 to 377.17, all closed in place in Slice 377. Slice 408 grilled 377.11
and 377.12, and 377.13 onward are new. Label 381 is 381.1; 409 and 410 are the
two Standardize sweeps. Shipped lines were counted per subject, with `git diff
--numstat <sha>^ <sha> -- packages/ apps/docs/src` over each subject's own
commits:

| subject | commits | shipped lines | grill |
|---|---|---|---|
| 377.13 | `908cf7c9` | 0 | narrowed |
| 377.14 | `f7cecafb` | 89 (`lib/shell-band.ts`, six pages and demos) | **full** |
| 377.15 | `20dc0918`, `26b262ce` | 0 | narrowed |
| 377.16 | `4e9308c9` | 8 (`patterns/object-page.astro`) | **full** |
| 377.17 | `0205933f`, `9eedab1f` | 0 | narrowed |
| 381.1 | `59daa442`, `68b2f088` | 0 | narrowed |
| Slice 409 | `bb80a2ea` | 0 | narrowed |
| Slice 410 | `ec482111` | 0 | narrowed |

The first run of that count read 0 for every subject, with empty commit lists.
Two instrument defects caused it: `\|` in `grep -E` is alternation, and zsh
does not word-split `$shas`. It was redone in Python. It now reads non-zero
exactly where the subjects shipped, and 0 where they only touched loop
tooling.

**Jev was not consulted.** LOOPS §6 step 4 lets verifiers ask after their own
verdict. The owner paused Jev sends on 2026-09-27 while the jev 0.10
migration was reviewed, and this repo was not yet `jev allow`ed. So the first
run was stopped before any verifier started, and it was relaunched with the
consult removed. The verifiers' verdicts are their own.

## 1. 377.14 (full) — 21 claims checked, 17 hold; 6 findings

The headline figures reproduce:
- 7 documents, 8 page-blocks, 18 quantity and 12 money;
- 31 seams against the old 20, with 33 added checks;
- `shell-band.ts` throws on 0 or several bands;
- the band needle fails on real restatements in 8 injected spellings;
- the `MAY_BE_ABSENT` stand-down exits 0 only in the container, and CI's full
  checkout verifies all three files on every push.

The critic re-derived the 7 / 18 / 12 census with an enumeration that shares
no code with the finder or the verifier, and it agrees.

| id | severity | verdict | finding |
|---|---|---|---|
| 377.14-F1 | medium | NARROWED | (d) named two live docs defects and filed neither. The richtext page still says its align controls "sync `aria-pressed`", while the rendered controls are `type=radio` with no `aria-pressed`, and check-claims asserts the opposite. Only the richtext half reproduces as stated. |
| 377.14-F2 | low | CONFIRMED | Nothing floors the derived seam list. Cutting it to `/components/` pages (8 runs → 2, 31 seams → 20) keeps every case green, including the one named "and no fewer runs". Its second conjunct follows from the first. |
| 377.14-F3 | low | CONFIRMED | The DONE block says seven restatements now print from the lib. Six do, and one was deleted. The deletion was correct, because the docs rail measures 0px at the band, not an icon rail, but it was not recorded. **Corrected in place.** |
| 377.14-F4 | low | CONFIRMED | The band needle misses `56REM`, `896PX`, `896&nbsp;px`, `56&#8239;rem` and `896 pixels`. CSS units are case-insensitive, and Chromium's `matchMedia('(max-width: 56REM)')` matches. |
| 377.14-F5 | low | CONFIRMED | The sidebar-nav captions print the derived band, but the demo frames that sit on each side of it are hand-typed (40rem, 60rem) and unchecked. |
| 377.14-F6 | low | CONFIRMED | (d)'s extractor and labels stayed in session scratch. Its 762, ~635 ± 96 and 18.5% cannot be re-derived. Only the corpus (81 = 41 + 40) and the 289 call sites reproduce. |

## 2. 377.16 (full) — 17 claims checked, 11 hold; 7 findings

The fix works on the property it named: no overlap, and the first anchor is
reachable at 390, in LTR and RTL. Print is unaffected, because the strip is
`display: none` on paper at 794 and at 390, with or without the revert. What
it shipped beside that is the finding.

| id | severity | verdict | finding |
|---|---|---|---|
| F1 | medium | CONFIRMED | **A regression 377.16 introduced.** At 390 and 320, as the reader scrolls the page, the `aria-current` label scrolls out of the strip. The strip never scrolls to it: at 390, "Document flow" shows 28.4 of 101.7px and "Approvals" 0 of 67.8, where both were fully on screen before the fix. Two instruments and two revert methods agree. |
| F2 | medium | CONFIRMED | Tab onto a partly visible anchor does not scroll the strip. "Document flow" is focused with 30 of 113.3px visible at 390, in every row size. Chromium reveals only a fully hidden link. |
| F3 | medium | CONFIRMED | The strip's scroll container clips every link's focus ring top and bottom, and the first link's left side too. A pixel diff shows one 2 × 40px bar on the first anchor. **This predates 377.16**: the revert gives the same clip. |
| F4 | medium | NARROWED | The copyable Markup has 2 links and renders clean. With the demo's 5 links, which is realistic but not "as-is", the published CSS reproduces the overlap 377.16 fixed, because the fix lives only in page-scoped CSS. |
| F5 | low | CONFIRMED | The new check-claims case's geometry is LTR-only. Under `dir=rtl` on the fixed build it reports a false red (overlap 219.7 and two unreachable labels), so it cannot guard the RTL form of the bug, which existed before the fix. |
| F6 | low | CONFIRMED | At 320 (the ZOOM_400 width) the page's States table overflows the main pane by 12px comfortable and 52px spacious. It is the one `bo-data-table` outside a container. This predates 377.16. |
| F7 | low | CONFIRMED | Where scrollbars are overlay (0px, also with `--hide-scrollbars` removed), nothing but a label cut mid-word shows that the strip scrolls. At 320 only two labels are fully visible. |

## 3. Narrowed subjects — 25 headline claims, 24 hold; 2 findings

What held:
- **377.13:** `report:grid-presses` reads 0 of 4,910 over 132 pages, and the
  4,944 cannot be reconstructed, as recorded.
- **377.15:** 163 + 197 = 360 in CI; every top-level case sits in exactly
  one part block; wall 3.9-4.0 min. The critic confirmed "free": the repo is
  public, and all 4 jobs run on `ubuntu-latest`.
- **377.17:** 5 consecutive part-B passes, attempts 1-5 of `36249444686`.
- **381.1:** floor first; 234 → 84; 4 of 84; 12 / 17 of 18; 20 of 20
  mutations killed. The critic took a **second reading** of 15 of the blind
  judge's 84 verdicts. 14 agree FALSE, and one (h19) is borderline.
- **Slices 409 and 410:** dispatch region 10,374 → 10,399 → 10,374, and lane
  2's inputs unchanged.

| id | severity | verdict | finding |
|---|---|---|---|
| R-F1 | low | NARROWED | 377.17's case passes when the input's click is cancelled (`preventDefault`) and no picker opens: `activated: 1`, `chooserSeen: false`, even with a 10s wait. A trusted click is necessary for a picker, not sufficient, and the case's label claims the picker. |
| R-F2 | low | CONFIRMED | 377.15's three secondary timings do not match the runner API. The last unsplit run's wall was 6.55 min (`36244315651`), not 6.4. 6.4 is a job duration or an older run. The shard ranges were 206-237s and 134-196s. The headline holds. **Corrected in place.** |

## 4. Completeness critic — 6 gaps, each run

- The 381.1 second reading and the 7 / 18 / 12 re-derivation (above) both
  hold.
- Print on 377.16 holds.
- 377.15's "free" holds.
- 68b2f088's documented invocation, `check_correction_sites.py --worktree
  --old`, works. It listed 3 sites for "4,944", all quotations, matching
  `grep -c -F 4,944` = 3. The rule's "It reports and never fails" means "not
  a gate". It does exit 1 when it lists something, which is its report code.
  Left as written.
- The critic noted that no hunt did the owner's 2026-09-27 request (the jev
  0.10 recheck). That request was never this grill's scope, and it runs as
  its own workflow.

## 5. Thesis section

- **Adoption**: quoted from `python3 scripts/loops/record_metric.py
  --adoption`, taken 2026-09-26 23:54: 20 metrics recorded, 0 channels
  unreadable.
  - npm `@busy-office/ui` 990 in the window 2026-08-26..09-24, 135 outside
    publish days.
  - `create-ui` 651 and 84.
  - jsDelivr 30 hits and 23, 0 to `dist/`.
  - GitHub: stars, forks, watchers, non-owner issues and PRs, and
    discussions all 0; 20 views and 367 clones over 14 days.
  - Each npm count is +1 against Slice 408's 18:17 reading. That is within a
    day's noise. 0.9.0 still cannot show, because it was published after
    npm's window ends.
  - What no channel sees: docs readership, private proxies, the CI and mirror
    share of npm, traffic beyond 14 days, and a copied `dist/`.
- **First user**: busy-office-erp **moved, on paper**.
  - `git log --all --since=2026-09-10` counts 6 commits (Slice 408 read 2).
  - `21c0c14` (09-26 22:13) records `@busy-office/ui` 0.9.0 in its ADR-0016
    and runtime UI contract. The licences now match (Apache-2.0 from 0.9.0),
    and it states "0.9.0 still has 40 components and no `kanban` or
    `report`". That statement is **true**: `git ls-tree` of `v0.9.0`'s
    `components/` lists 40 directories, and none is kanban or report. Kanban
    is a pattern page here, not a component.
  - ADR-0016 is still **"Proposed"**, and no UI code in that repo imports the
    framework yet.
  - `main` is `89261b0` (22:41, ADR-0021 realign).
- **Comparators**: re-read with `npm view <pkg> version time.modified`:
  fundamental-styles 0.41.9 (09-17), `@ui5/webcomponents` 2.27.2 (09-25),
  `@patternfly/react-core` 6.6.1 (09-24), `@carbon/styles` 1.116.0 (09-23).
  - The versions are the same as Slice 408's, so its comparison stands:
    none of the four ships CSS, delegated behaviour and an htmx story in one
    package.
  - The floors to beat remain `prefers-contrast` themes, five row sizes and
    a weekly release cadence.
- **Framework code**: `git diff --numstat 95f31c4d HEAD -- packages/core/src`
  reads **0 lines** (no output) since Slice 408's grill commit.
  - Docs source moved +61 −36 (`-- packages/ apps/docs/src`).
  - The one user-facing change in the window is 377.16's page CSS, and this
    grill found it regressed the marker it did not measure (F1).

## 6. Triage (Slice 411)

Filed as open items with Accept criteria:
- 411.1: the anchor strip's marker tracking, Tab reveal, focus-ring clip and
  overflow cue (F1, F2, F3, F7);
- 411.2: the published CSS under the copyable Markup (F4);
- 411.3: the case's RTL geometry (F5);
- 411.4: the States table at 320 (F6);
- 411.5: the richtext align sentence and the output-form wording
  (377.14-F1);
- 411.6: the seam-list floor (F2);
- 411.7: the band needle's spellings and the sidebar-nav demo frames (F4,
  F5);
- 411.8: (d)'s instruments (F6);
- 411.9: the file-picker `defaultPrevented` gap (R-F1).

Corrected in place: 377.14's "seven" (F3) and 377.15's secondary timings
(R-F2).
