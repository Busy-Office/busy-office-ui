# Objective grill — 388.1, 389.4 / 389.25, Slice 390 (2026-09-25)

Rule 3, armed `[388, 389, 390]`, labels resolved by commit subject: 388 is item
388.1 (`a1b3969b`), 389 is item 389.4 (`1b19d6e9`, with 389.25 closed in
388.1's commit), 390 is Slice 390 (`1d0046c3`). **Narrowed out:** 388.2, itself
a grill whose 25 findings a challenger already re-measured. **Dropped by the
counter, named here (§6 step 0):** the armed set `[372, 375]` — 372.1's
closure (`f866ad11`) and 375.11-partial (`0c6f319c`, +46 lines of shipped
data-table CSS) — was discarded when 388.2's design-grill was logged as an
Objective row. Neither is grilled here; 375.11's remaining half re-arms 375
when it lands, and 392.4 decides whether a design-grill may reset rule 3.

Method: a workflow of four finders (scan 389.4, button 388.1, sweep 390,
thesis), each followed by an independent skeptic with its own scripts.
Scored: **41 of 59** load-bearing claims reproduced; **28 findings, 22
survived**, plus 12 the skeptics found that the finders missed (counted from
the workflow journal). The table below groups the survivors that change
something; the rest are recorded in the scratchpad journal and were low.

## Thesis section (LOOPS.md §6 Exit)

- **Adoption, with windows.** npm `@busy-office/ui`: 16 last week
  (2026-09-15..21 — the same week Slice 384 read, so not a new sample), 5 of
  them 0.8.0; last month 1,480, of which 1,308 (88.4%) fell on the four publish
  days; since the last publish 62 in 15 days. `create-ui` 12 / 650. jsDelivr:
  29 hits a month, 0 on any `dist/` file. GitHub: 0 stars, 0 forks, 0
  non-owner issues or discussions; traffic 20 views / 6 uniques, clones
  367 / 132 (239 on 09-23, when 42 Actions runs ran). **What no channel sees:**
  the docs site has 0 analytics, so readership is invisible; npm counts
  include CI and mirrors. Adoption is indistinguishable from zero.
- **The named first user (377.6).** busy-office-erp (private, last push
  2026-09-09) has no package.json; spike 6 pins `UI_VERSION = "0.8.0"` and
  vendors `index.min.css`, `htmx.min.css` and `api.json`, validating with
  `check-markup.mjs`. ADR-0016 is **Proposed**. Its contract's 27 classes
  are all declared in 0.8.0 and in HEAD. Its one tracked ask, issue #2 (a
  kanban board), was refused on 2026-09-07 (300.2) but the issue was never
  told: it is still OPEN with only the triage comment. **Owner action.**
- **Comparator: SAP fundamental-styles 0.41.9.** 100,355 downloads last month
  (68x ours), 121 components (ours 40 + 5 primitives). **Evidence** deltas:
  BO ships runtime behaviours in the package (24 behaviour files; FS 0, which
  sends behaviour to fundamental-ngx/react/vue); FS ships author-selectable
  high-contrast themes (4 HC of 12 theme files; BO has forced-colors in 26
  files but 0 `prefers-contrast` and only light/dark); BO has forced-colors
  rules (26) where FS has 0. **Refuted** as BO differentiators: generated
  AI docs (FS ships llms.txt, a component catalog, schemas and a 17-tool MCP
  with 107 weekly downloads) — BO's residual edge there is a markup
  VALIDATOR; and "spacious" density (SAP cozy, the default, is 36px / 44px,
  as BO's comfortable / spacious; SAP condensed goes to 22px, below BO's 28px).
- **Framework code since the last Objective row** (`e76a8f90`):
  `git diff --numstat e76a8f90 HEAD -- packages/core/src` -> 2 files, +34 -5
  (button.css, scan.css). This instrument cannot see shipped code outside
  `src` (the `bo-check-markup` bin, build scripts).
- **377.5 has grown:** 16 Fixed entries under Unreleased, seven from P0 items
  (300.1, 375.9, 375.10, 376.2, 376.4, 377.1, 389.4), 313 commits past
  v0.8.0 — but 0 of the six CSS/JS P0 fixes touch the first user's committed
  screens. Corrected in 377.5; the release stays the owner's call.

## Findings that survived, and what was done

| # | Finding | Severity | Done |
|---|---|---|---|
| 1 | A verdict stamped inside a live flash does not restart it: a server rejection 250-620ms after an ok scan shows no frame and no wash (peak opacity 0.106 -> 0); rescans every 650ms never flash again. The documented goods-receipt 404 path hits it, and with 389.3 open nothing else on screen shows the rejection. Predates 389.4 (126.2's structure) but sits inside its property. | P0 | filed 392.1 |
| 2 | Pressed toggle is colour-only in normal colours: fill 1.04:1 light / 1.36:1 dark, text 2.34 / 1.42; the richtext toolbar's Bold is the same pair. The new caption claimed the opposite. | high | caption corrected; filed 392.2 |
| 3 | A `--bar` label with one word wider than its slot paints past its button (15-49px at 390); "a long label WRAPS … content never clips" is false for single words. `overflow-wrap: anywhere` fixes it in the skeptic's run. | medium | filed 392.3 |
| 4 | Two Objective rows reset rule 3 without §6's thesis section (386 read no channel; 388.2 is a design-grill), and 388.2's reset dropped `[372, 375]`. | high | scope named above; filed 392.4 |
| 5 | 388.1's first refusal ground ("8 of 8 pills are statuses, never an action") is false: 2 are remove buttons and `.bo-chip` is a link on 117 of 128 pages. The other three grounds reproduced; the refusal stands on them. | medium | corrected in 388.1 and on the page ("never a command button") |
| 6 | 377.5's consumer premise wrong, and its counts stale (six -> seven P0, 260 -> 313 commits). | high | corrected in place |
| 7 | Slice 390 lane 4 called both +26 "new instruction"; rule 3's is 384's replay-figure provenance (384.1's generator). | medium | corrected in place |
| 8 | CHANGELOG "visual change only" omitted that forced colours no longer fire animation events. | low | corrected |
| 9 | `/components/scan` still stated the 40 kB RF budget. | low | corrected |
| 10 | 388.1's "+6 claims" is +5. LOOPS.md enumeration had a doubled "and". | low | corrected |
| 11 | The 389.4 pixel claim reads only the peak frame, and one branch clears its 1.1 bar by 0.02; the one-shape claim's density loop is identical by construction. | low | recorded, not filed |
| 12 | The 41 kB RF budget raise pays for a rule no RF screen uses. | low | recorded; 392.2's fix should weigh it |
| 13 | Comparator deltas are not recorded anywhere a later grill reads them. | medium | filed 392.5 |

**Refuted by the skeptics:** the EDGE_EXEMPT wording overstating duration
(the substance holds), the adoption-jsdelivr-month row "holding the quarter",
"lane 1's equal-to-base compares an unquoted figure", and the finder's own
environment premise. **Not re-run:** real Windows High Contrast (emulated
only); Firefox and WebKit.
