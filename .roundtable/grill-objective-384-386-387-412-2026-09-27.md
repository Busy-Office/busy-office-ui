# Objective grill — 384.1, 386.1, 387.1, Slice 412 (2026-09-27)

Slice 413 in ROADMAP.md. Method: finders, refute-first verifiers, a completeness critic;
384.1/386.1/412 narrowed to headline reproduction (0 lines shipped under `packages/`,
`apps/docs/src`).

## 387.1 (full)
11 claims: 9 hold, 2 fail.
- FAIL "within the six-line reserve": 390px, last column, 117 chars, message 83x262px
  (15 lines), `scrollHeight` 244 to 360 on focus; 303 chars gives 38 lines; 80 of 144
  cells breach. Pre-fix rules: 7-8 lines, no height change. A press after a vertical
  wheel while focused lands on TBODY (F1, F2: high, CONFIRMED, reproduced by the verifier).
- FAIL "cannot lengthen the scroller": unbreakable token, scrollWidth 1200 to 1729,
  same as before 387.1 (F3, medium, CONFIRMED).
- F4 (NARROWED, unreproduced): no shipped RF page uses the fallback; at Chrome 108 the
  `lh` unit is invalid so the reserve declaration is dropped.
- F5 (CONFIRMED): the sticky-bar half of the pasted-markup case cannot fail under
  overlay scrollbars. F6 (NARROWED): `display: block` loses the 4px label/hint gap.
  F7 (CONFIRMED): "2 of 7" vs "4 of 7" unlabelled samplings.
- Holds: before/after figures of both paths, the four cases exist, rf-essentials
  41,966 of 41,984 bytes, `position-area` versions.

## 384.1 / 386.1 / 412
384.1 F1 (NARROWED): floor basis unstated; region words pass, file words do not.
Jev was not consulted. 386.1 and Slice 412 reproduce.

## Critic
- RTL on the no-anchor fallback: no defect (ltr and rtl both sw 340, overflow 0).
- check-claims never reads `passedThrough`: gap confirmed by reading; red-proof owed.
- Size max raised a third time for data-table: not judged, filed in 413.4.

## Not covered
Firefox and Safari (single-engine simulation); Jev readings (none taken on 387.1).

## Thesis section
Readings taken 2026-09-27 03:44.
- **Adoption:** npm `ui` 980 downloads in the month, 125 outside publish days; `create-ui` 652; jsDelivr 30, 0 to `dist/`; GitHub 0 stars/forks/issues, 20 views, 367 clones.
- **First user:** busy-office-erp has 18 commits since 09-10, main `0c82b9a`; ADR-0016 is still Proposed.
- **Comparators:** unchanged (fundamental-styles 0.41.9, ui5 2.27.2, PatternFly 6.6.1, Carbon 1.116.0).
- **Framework code:** `git diff --numstat 94450fd5 HEAD -- packages apps/docs/src` gives 77 +/30 − in `data-table.css` (plus 2 +/1 − in `check-size.mjs`), i.e. 110 lines; the data-table lines are the fallback of a path no shipped RF page exercises (F4).
- **Reading:** the wake's framework work went to a fallback for browsers below Chrome 129 while adoption is single-digit outside publish days; 413.1 should weigh that before more fallback code.
