# Objective grill — 362.1, 369.2, Slice 385 (2026-09-25)

Rule 3, armed `[362, 369, 385]`; labels resolved by commit subject (item, item, slice).
Scored: 3 headline claims reproduce; 4 defects found, all in Slice 385's own write-up.

## Thesis section (LOOPS.md §6 Exit)
- **Adoption reading:** not taken this grill — npm downloads and the GitHub
  issue/discussion intakes were not read. Named so the gap is visible.
- **First user:** unchanged; 377.6 (is busy-office-erp the named first user) is
  still an owner call.
- **Comparators:** SAP fundamental-styles not re-read this grill.
- **Framework code since the last grill (Slice 384, `eec86ae2`):**
  `git diff --numstat eec86ae2 HEAD -- packages/core/src` -> 0 files. All three
  items are docs/build/records; no shipped CSS or JS moved.

## Claims that reproduce (each with its instrument)
| Claim | Instrument | Result |
|---|---|---|
| 362.1 `astro check` is a gate at 0 errors | `npx astro check`; `check:types` inside `docs` `build` | 0 errors, 26 hints |
| 362.1 stray `))}` gone from /reference/tokens/ | tags/script/style stripped, count in visible text; control: pre-fix source `2fc94372^` | 0 visible; control has the line. A raw `grep -c` reads 1 — minified JS, wrong instrument |
| 369.2 print reset reaches every page | puppeteer, print emulation, both themes, all 139 built `index.html` | 0 of 278 off `#fff`/`#000`; earlier red-proof (rule deleted at runtime) reverts on the ten |
| 385 archive move is lossless | independent regex reader per slice; checkbox count | 21/21 byte-identical; 925 = 925; open 33 = 33 |

## Defects (all corrected in ROADMAP.md, Slice 385 entry)
1. Share "43.6% -> ~0" was never measured. `roadmap_scope.py`: **14.4%** (971 lines, 6 closed slices kept).
2. "~6,780 lines" — measured 6,750.
3. Lane 2 "unchanged by construction" — false: `package-lock.json` is a lane-2 input and moved 796 lines. The figure held (74·245·234·7); that is the accurate claim.
4. "+170 words is that, not drift" — attribution unmeasured; softened.

## Filed
386.1 — no gate keeps the print reset true on a new standalone page (base rate 0 of 139 today).

## Not covered
Dark-theme print contrast (369.1, owner call); rendering in non-Chrome engines.
