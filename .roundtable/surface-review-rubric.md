# Slice 37 rubric — how a component or pattern gets scored

Written and committed **before any item is scored**, so it cannot be fitted to a
conclusion after the fact. Owner input welcome on the weights and the outcomes
before 37.2 runs; changing it later is fine, but it changes for everything, not
for the one row it would be convenient in.

The measured inputs live in [`surface-baseline.md`](./surface-baseline.md),
generated by `scripts/surface-signals.mjs`.

## The one rule that makes this worth doing

**Every score cites something checkable.** A file, a count, a gate, a screen. If
the only support for a number is that it seems right, the number does not go in
— write "unscored, needs X" instead. This project has spent five slices on
confident output that nothing verified; a 55-row table of opinions would be the
largest instance of it yet.

## Dimensions — 0-3 each, 12 total

### 1. Demand — does a real back-office screen need it?

Measured: how many of the 17 shipped screens (16 pattern pages + the
`examples/po-app` reference) use it.

| | |
|---|---|
| **3** | Used by 7+ screens, or by the reference app. Load-bearing. |
| **2** | Used by 3-6 screens. |
| **1** | Used by 1-2 screens. |
| **0** | Used by none. |

**A 0 is a question, not a verdict.** Three readings, and the review must say
which applies: (a) genuinely unneeded, (b) needed by a screen this repo has not
written yet — real ERPs page through lists whether or not `invoice-list` does,
(c) a screen that *should* use it is quietly doing without, which makes it a
defect in the screen. Only (a) leads toward removal.

### 2. Composition — could existing primitives already do this?

**Not measured — judged, with the composition written out.** If the answer is
"yes, it is a card plus a table plus a badge", say so in those words and the
score is low. This is the "less for more" principle: one component with many
settings beats N components.

| | |
|---|---|
| **3** | A genuine primitive, or it absorbs a hard problem (focus, precision, save semantics, density) the consumer would otherwise solve wrongly. |
| **2** | Composable in principle, but the composition is subtle enough that most consumers would get it wrong. |
| **1** | Mostly a preset arrangement of existing parts. |
| **0** | Three existing components in a trench coat. |

### 3. Contracts — does it honour the framework's own promises?

Density, two-channel state, RTL, print, forced-colors, target size. Each is
already gate-measurable; the baseline table carries the cheap signals.

| | |
|---|---|
| **3** | Every contract that applies to it is honoured and gate-covered. |
| **2** | Honoured, with a gap that is stated on its page. |
| **1** | A contract that clearly applies is unhandled. |
| **0** | It contradicts one — colour-only state, a fixed size that breaks density. |

**A blank column is not automatically a gap.** A component with no colour of its
own needs no `forced-colors` rule. The review says which contracts *apply*
before scoring whether they are met.

### 4. Evidence — is its documented behaviour actually verified?

| | |
|---|---|
| **3** | Executable claim(s) drive it in a browser, plus gate coverage. |
| **2** | Gate-covered (axe, contrast, target-size) but no behavioural claim. |
| **1** | Static checks only. |
| **0** | Its page asserts runtime behaviour that nothing executes. |

A **0 here is the cheapest to fix and the most urgent** — the 409/htmx page and
the nine-tabs-one-panel bug were both this, and both shipped looking correct.

## Outcomes

Every row ends in exactly one, with a reason:

- **Keep** — recorded so the next sweep does not re-litigate it.
- **Improve** — becomes a queued roadmap item with its own Accept criteria. Not
  "could be better"; a specific change.
- **Merge** — into which component, and why the result is *less* surface. If the
  merged thing needs more settings than the two it replaces, it is not a merge.
- **Deprecate** — see the constraint below.

### Deprecation is not deletion

`@busy-office/ui` is published: 0.1.1 is live, 0.2.0 is tagged. Removing a class
in a minor breaks consumers silently, which is precisely what this framework
claims not to do. "Deprecate" therefore means, in order:

1. a **Deprecated** entry in the CHANGELOG,
2. the docs page stating what to use instead, and
3. the class removed **at the next major** — not before.

A low score is never sufficient on its own to remove something a consumer may
already ship. The bar for removal is that the replacement is documented and
reachable.

## Scoring order

Batches of 8-10, worst-measured-signal first, so the rows most likely to change
something get attention while judgement is freshest. Patterns are scored on the
same four dimensions, reading "screens" as "does this describe a screen a
back-office user actually sits in front of".
