# Objective grill of 377.4 (full), with 376.7, 377.7 and Slice 402 narrowed (2026-09-26)

Dispatched by rule 3 at `Objective 3 / 3 slices OVERDUE [376, 377, 402]`. The
last reset was Slice 401 (`2888ce5c`). Eight agents ran in one workflow
(`wf_b0e585a0-7c6`), read-only on the repo:
- three narrowed reproducers;
- two defect hunters on 377.4;
- refute-first verifiers on both hunters' findings.

Raw returns: session scratchpad `grill-403/groups.json`.

## 1. Scope

| label | resolves to (row's commit) | lines under `packages/` + `apps/docs/src` | grilled |
|---|---|---|---|
| 376 | 376.7, the ratchet floor (`3569a013`) | 0 | narrowed (381.2) |
| 377 | 377.4, the pointer-coverage gate and 7 trusted cases (`1f34a912`) | 4, a generated README stamp | **full**. Its gate and cases are now build-trusted, so a defect hunt is owed even though the shipped-line count is tiny. |
| 377 | 377.7, the adoption instrument (`dfc9bf54`) | 0 | narrowed |
| 402 | the Standardize sweep (`da1b2cb8`) | 0 | narrowed |

None of the four had been grilled before.

## 2. Claims reproduced

39 claims were re-run by their own commands: **33 yes, 4 partly, 2 no**.

| group | claims | yes | partly | no |
|---|---|---|---|---|
| 376.7 (narrowed) | 7 | 6 | 0 | 1 |
| 377.7 (narrowed) | 4 | 4 | 0 | 0 |
| Slice 402 (narrowed) | 9 | 9 | 0 | 0 |
| 377.4 gate (hunt) | 11 | 8 | 2 | 1 |
| 377.4 cases (hunt) | 8 | 6 | 2 | 0 |

**The two that do not reproduce are both the dispatcher's own DONE-note
claims:**
- **376.7's "196 shrinks" is 188.** The module's own functions, a shell
  `wc -w` walk and an independent walk all read 188, and history variants
  range 186-203, so the instrument can differ. The 196 was a `wc -l` over an
  output file holding 188 shrink rows plus 8 per-file header lines: a line
  count quoted as a shrink count. The floor's argument stands on the 188.
- **377.4's "`page.touchscreen.*` counts as trusted" is false.** The regex
  has no touchscreen alternative. The combobox touch case at
  check-claims.mjs:1895 is credited by another line in its block.

**Also corrected:** 376.7 called `f7bc8777` "the revision where this item was
filed". 376.7 was filed in `85e8c6f5`, and `f7bc8777` is the close that the
old code named as the last cut. The replay at `85e8c6f5` gives the same
answer: old `f7bc8777`, 8 up; new `3cb2381a`, 83 up.

## 3. Findings (13), after refute-first verification

**None is a P0. 6 are confirmed, 7 narrowed and 0 refuted.** The code
findings go to 403.1 (the gate) and 403.2 (the cases). The doc-claims were
corrected in place in this commit.

| id | verdict | severity | what |
|---|---|---|---|
| gate-F1 | confirmed | defect | The trusted-call regex matches comments, strings and multi-line `page.evaluate` bodies (where `evaluate(` is on an earlier line). An annotated synthetic-only case can therefore pass. It fails open on the one regression the gate exists to catch. No verdict at HEAD is wrong today. |
| gate-F2 | confirmed | minor | An annotation's 150-line block ignores case and `visit()` boundaries. A stray annotation is satisfied by another case's press 104 lines later. |
| gate-F3 | confirmed | minor | Listener detection decides alone what it failed to see. It never reconciles raw `addEventListener` registrations against the ones it classified, and it does not scan `utils/` or non-`.ts` files. There are 0 real misses today. |
| gate-F4 | confirmed | doc-claim | Touchscreen and puppeteer handle and frame clicks are unrecognised. These misses fail closed. The DONE note and the commit claimed touchscreen. |
| gate-F5 | narrowed | doc-claim | "Every one of their cases was an in-page `el.click()`" is false for table-toolbar. Trusted clicks already reached its `change` listener, and only the export click branch had no case. |
| gate-F6 | confirmed | minor | The gate's scope is pointer events, so a pointer path that arrives as `change` is out of scope. That includes data-grid's documented trusted-only divergence (278.4). Its case could regress to `el.click()` and the gate would stay green. This matches the Accept as written: a scope gap, not an implementation bug. |
| cases-F1 | narrowed | defect | The validation-summary trusted case cannot tell the handler from Chrome's own fragment navigation, which also opens `<details>` and focuses. Outright deletion of the handler is caught elsewhere, by the synthetic `#vs-note` case and vitest. |
| cases-F2 | confirmed | defect | The row-edit trusted case never asserts where focus lands after a real press. A hide-before-refocus regression passes both it and vitest. |
| cases-F3 | narrowed | nit | collapsible-card reads `data-state` but never asserts it. Other cases catch the regression. |
| cases-F4 | narrowed | nit | tag-input never checks WHICH chip was removed. Three other cases catch that. |
| cases-F5 | confirmed | minor | The isTrusted recorder cannot tell a pointer press from keyboard activation. A partial swap to Enter passes both the case and the meta-gate. |
| cases-F6 | narrowed | minor | The recorder counts a behaviour's own synthetic clicks, such as `reveal()`'s `owner.click()`. A trusted press on the path that would discriminate therefore false-fails `allTrusted`. |
| 376.7-F1 | confirmed | doc-claim | 196 is 188, as above. |

## 4. What this grill does not cover

- **Browsers:** Chromium only.
- **The seven cases:** re-run and mutated in a scratch harness. The
  commit's own two-injection red-proof was not recorded exactly and could not
  be re-run; seven fresh mutants each failed exactly one case.
- **377.7:** the network readings are live values, so "reproduces" means the
  same channels, windows and exclusion behaviour, not frozen numbers.

## 5. Thesis section

- **Adoption**: quoted from `python3 scripts/loops/record_metric.py
  --adoption`, taken at 2026-09-26 13:30 with 20 metrics recorded and 0
  channels unreadable. This is 377.7's instrument, used as its Accept asked.
  - npm `@busy-office/ui`: 989 in the window 2026-08-26..2026-09-24, npm's
    newest computed day. 134 fell outside the three publish days, a median of
    3 a day.
  - npm current version 0.9.0: 0, because it was published 2026-09-26, after
    the window ends, so it cannot show yet.
  - `@busy-office/create-ui`: 650, of which 83 fell outside publish days.
  - jsDelivr: 30 hits, 0 of them to `dist/`. For create-ui, 23 and 0.
  - GitHub: stars, forks, watchers, non-owner issues and PRs, and
    discussions are all 0. Traffic over 14 days (2026-09-10..23): 20 views
    from 6 uniques, and 367 clones from 132 uniques, with a peak of 239 on
    09-23, a CI day.
  - What no channel sees: docs readership, private proxies, the CI,
    mirror and first-user share of npm counts, traffic beyond 14 days, and
    copied `dist/`.
- **First user**: busy-office-erp is unchanged since Slice 401's reading.
  `git log --all --since=2026-09-10` counts 0 commits; `dev` is at `3e122b6`
  (2026-09-10 05:57 +0800); ADR-0016 is still "Proposed"; its spike vendors
  0.8.0. 0.9.0, with the relicence it asked for, shipped today, and it has not
  moved.
- **Comparators**: from SAP fundamental-styles, re-read with `npm view <pkg>
  version time.modified`:
  - fundamental-styles 0.41.9, last modified 09-17;
  - UI5 Web Components 2.27.2 (09-25);
  - PatternFly 6.6.1 (registry modified 09-25);
  - Carbon `@carbon/styles` 1.116.0 (09-23).

  All are unchanged since Slice 401's reading today, so its comparison
  stands: none of the four ships CSS, delegated behaviour and an htmx story
  in one package. The floors to beat remain `prefers-contrast` themes, five
  row sizes and weekly cadence.
- **Framework code**: `git diff --numstat 2888ce5c HEAD -- packages/core/src`
  reads 1 file, +1 −1, over 10 commits. That file is
  `src/data/readme-facts.json`, the gate-count stamp, which is data. **No
  framework code changed since the last grill.** Every subject was machinery
  or test harness. The command cannot see the harness itself
  (`apps/docs/scripts/`), which is where 377.4 lives.

## 6. Round-table (Hypothesis: one source, this reading)

- **ERP team lead:** there is nothing new to adopt since this morning, because
  the day's work since 0.9.0 is test coverage and loop instruments.
- **Adoption skeptic:** the adoption instrument now measures honestly, and it
  measures zero. Two grills in one day on 0-line framework windows are the
  loop inspecting itself.
- **Maintainer:** 377.4's cases are real value, because trusted input now
  reaches 16 of 16 pointer behaviours. But the next unit of progress is
  product or first-user conformance (O5), not a third machinery grill.

## 7. Triage

- **Filed as Slice 403:**
  - 403.1, gate hardening: gate-F1, F2, F3, F4 and F6;
  - 403.2, case assertions: cases-F1, F2, F5 and F6, plus the two nits.
- **Corrected in place:**
  - 376.7's DONE note and its code comment (196 is 188; the filing
    revision);
  - 377.4's DONE note (touchscreen; table-toolbar).
