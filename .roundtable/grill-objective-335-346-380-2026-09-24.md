# Objective grill — 335.1, 346.1, Slice 380 (2026-09-24, local wake)

**Dispatched by rule 3** (`Objective 3 / 3 slices OVERDUE [335, 346, 380]`,
`dispatch_status.py`) after rule 1 read 0 open P0 and rule 2 read `0 / 4`.

**Scope, resolved label by label** (§6 step 0). 335 and 346 are item ids, not
slices: the rows led with `335.1 — …` and `346.1 — …`, and both items were
closed in place (`0756cafb`; `f8856986` plus follow-up `2428af86`), not by a
new slice. So the subjects are those two closures and Slice 380 (`741c9bea`).
No earlier grill covers any of them (`grep -hoE '^## Slice [0-9]+ — Objective
grill of Slices'` over both roadmap files).

**Method.** Four finders: one per subject, plus the product thesis. Each was
followed by an adversarial verifier told to default to *does not survive* and
to re-run at least two held claims on its own. A finding is **Evidence** only
with two independent sources; otherwise it is a **Hypothesis**. Workflow
`wf_516a8fb5-83a`; each agent's result is in its `journal.jsonl`. Agents worked
read-only against the repository and GitHub, and built past revisions in their
own worktrees.

## Score

| subject | claims reproduced | findings | survived | refuted |
|---|---|---|---|---|
| 335.1's closure | 9 | 5 | 3 | 2 |
| 346.1's closure | 14 | 13 | 12 | 1 |
| Slice 380 | 11 | 5 | 3 | 2 |
| Product thesis | 11 | 10 | 9 | 1 |
| **total** | **45** | **33** | **27** | **6** |

Every verifier's spot-checks of held claims held (six, six, four, eleven).

**What held.** The headline of each subject reproduces:
- **335.1.** The intake reads `200 len 0` now. `/discussions/3` answers 404 on
  REST and NOT_FOUND on GraphQL. The owner's authorisation preceded the create
  by 44 s. No third party observably received anything: no watchers, hooks,
  discussion-triggered workflows or notifications.
- **346.1.** 1,062 commits; the 11-of-17 replay; 36 of 150 reporting, median 5
  and max 24.
- **Slice 380.** All four lane figures, and DESIGN.md's +1,388 words in 12
  commits.

The defects are in what shipped beside those numbers, as the last three grills
found. This time that includes **two tooling bugs** and **a verification recipe
that was not isolated**.

## 1. 335.1 — the proof held; the record overclaimed it

- **F1 (medium, Evidence): the intake as LOOPS.md spelled it did not run
  locally, and its 404 control cannot see why.** The block built
  `H="Authorization: bearer $GITHUB_TOKEN"`, which is unset locally, so both
  intakes answered `401 Bad credentials`. That is a JSON object, so `len` reads
  3, while the 404 control still answered 404. The closure ran `bearer $TOK`,
  then called it "this exact command"; the completion payload quoted the
  repaired form too. **Fixed:** the header now reads `${GITHUB_TOKEN:-$(gh auth
  token)}`, and each intake prints its status with a note to read the status
  before the length. Run locally with `GITHUB_TOKEN` unset, the block gives 200
  / 200 / 404. ENVIRONMENT.md §8 now says what actually ran.
- **F2 (medium, Evidence): "nothing is left in the repository" is false.**
  `/discussions/3` intermittently serves HTTP 200 with *"This discussion has been
  deleted."*, while never-used numbers answer 404. #3 is also consumed from the
  sequence issues share. The closure saw the 200 and checked removal by the
  absence of the title string, which a tombstone passes; CLAUDE.md's
  removal-verification rule forbids exactly that. **Corrected** in the DONE block
  and §8: no content is left, but not nothing.
- **F4 (low, Evidence): superseded sentences left standing.** LOOPS.md still
  called the red-proof "still owed", and RESUME.md still listed 335.1 as open
  and cloud-blocked, twice. **Fixed** in both files.
- **Refuted:** F3, "proved end to end generalises from n=1". The premises
  reproduce (the route is absent from GitHub's published REST description and
  accepts a bogus `state`), but the defect it infers was not shown. F5, "the
  403 still matters for answering", fails because the sentence is scoped to
  reading, and the untested REST write is already stated just above it.

## 2. 346.1 — the base rate held; the tool and its record did not

- **F4 (medium, Evidence): `record_iteration.py` checked HEAD, not the
  recorded commit.** A wake that records an earlier item with `--commit` never
  had that item checked; 21 of the last 300 record commits log a sha other than
  their parent. **Fixed:** it passes the recorded commit and runs from the
  repository root.
- **F8 (low, Evidence): the check failed silently.** From outside the repository,
  or with a bad sha, it printed "supersedes no number" and exited 0. **Fixed:**
  it locates its own repository, and a failed git read exits 2 with the error,
  which `record_iteration.py` reports as "could not run".
- **F2 (medium, Evidence): precision is about 2%.** Over the last 150 commits it
  printed 234 site lines, of which about 4 were real stale copies. The closure
  published volume without precision, in a file that warns a check crying wolf
  gets trained away. **Published now** (DONE block, LOOPS.md rule, docstring);
  **filed as 381.1**, re-tune or unwire.
- **F5 (medium, Evidence): the self-test does not guard the paths the published
  figures rest on.** 12 of 19 mutations survive it, including the
  commit-message path (drops L318) and struck extraction (drops L1470 and L812).
  **Filed in 381.1.**
- **F1 (medium, Evidence): an 18th stale copy.** `b7317ff9` L458, "at least three
  wakes' work", is superseded by "four", listed by the check itself, and was
  unannotated in the archive 13 lines below a 346.1 note. **Annotated**; the
  tally is 18 sites in 13 commits.
- **F3 (medium, Evidence): "none with a stale copy" is false for 2 of the 8
  commits the verifiers added.** `534c4593` and `411a6663` filed the correction as
  an open item and left the copy; the check misses both. **Corrected**, with the
  out-of-sample recall of 1 of 3.
- **F6 (medium, Evidence): the Accept's "command beside it" was not met.** No
  command or list was in the repository. **Fixed:**
  `.roundtable/measure-346.1-2026-09-24.md`. It carries the pinned selection (it
  reproduces 1,062 and 231 at `04e08b9b`, and the list is identical to the one
  read), the 59, the 18 sites and the helper.
- **F7, F10, F12 (low, Evidence): the figures drift as they are repeated.**
  "23 unbalanced `~~`" against "one stray", and 9,613 lines against a range
  covering about 10,700: the truth is one paragraph with an odd count, of 23
  markers, about 10,700 lines. "11 of 17" includes one site listed through a
  neighbouring figure: 10 on the superseded number itself. "Of 59" dropped the
  "at least, among the 231" qualifier. **All corrected** where they appear.
- **F11, F13 (low): shapes the check cannot see.** An end-of-hunk replace, a
  struck bare number, separators and units, and copies that moved to
  `ROADMAP-archive.md`. **Filed in 381.1**; the docstring now says it reads one
  file.
- **Refuted:** F9, "whitespace hid 3 of 17". Under the standard the count used,
  the aligned code-block line is findable by its number.

## 3. Slice 380 — the figures held, by luck in one case

- **F1 (medium, Evidence): the "clean HEAD tree" was not isolated.** With
  `node_modules` symlinked, `@busy-office/ui` resolves to the **main checkout's**
  `packages/core`, so `api.json`, `acr.json`, `ClassRef`/`ApiTable` and the copied
  framework CSS came from there. Two resolvers show it: Node's and Vite's. The
  figures held because the main checkout's `packages/core` had no uncommitted
  source (0 dirty paths under `packages/core/src`). **Fixed:** ENVIRONMENT.md
  §3c, with the check to run first, and RESUME.md's lane-run note no longer
  recommends the recipe.
- **F2 (medium, Evidence): lane 4's instrument recorded the relocation as a
  cut.** `last_region_cut()` counted any fall in region words. Slice 380's move
  reset the per-section baseline to itself, so the next sweep would read
  "0 of 16 moved", while what a wake reads grew by 19 words. **Fixed:**
  `is_cut()` requires the file to shrink too. That changes no past reading:
  `741c9bea` is the only region reduction on record where the file grew. The
  baseline is back at `4e6b83c1` (2 of 16 moved, +37), and the self-test gains 4
  cases; the old rule fails one.
- **F3 (low, Evidence): "None is incident narrative" is false.** `c073c360`
  adds a dated drift account, and rule 4's text carries a session aside ("78% of
  this session's shipped-source additions"). The "+674 build rules" bucket is
  two build rules and one unit-doctrine rule. **Corrected** in Slice 380 and in
  the 167.1 note. HONEST stands on the corrected ground: standing rules and
  decisions dominate.
- **Refuted:** F4, "the move's rationale is applied selectively". The paragraph
  that stays qualifies rule 4's own "read ROADMAP.md only". F5, "the region
  metric went unrecorded": true, but the series has been dormant across three
  sweeps and belongs to 353.2.

## 4. The product thesis (LOOPS.md §6 step 1)

**The reading** (taken 2026-09-24 by the thesis finder, read-only):

| channel | reading | window |
|---|---|---|
| npm `@busy-office/ui` | 16 | 2026-09-15..21 (npm's last complete week) |
| of which the current version, 0.8.0 | 5 | same |
| npm `@busy-office/create-ui` | 12 | same |
| jsDelivr | 29 a month, 41 a quarter; **0 hits on any `dist/` file** (README, package.json and a script only) | calendar month and quarter |
| GitHub traffic | 22 views from 7 uniques; 244 clones from 118 uniques | 2026-09-09..22 |
| stars, forks, non-owner issues or discussions | 0 | all time |
| latest published | 0.8.0 (2026-09-06) | — |

**What the channels cannot see:** copies of `dist`, private mirrors, the
owner's own busy-office-erp (a spike script pins 0.8.0; its ADR-0016 is still
*Proposed*; quiet since 2026-09-09), and agent use, the audience the owner's
direction names. Clones are the only channel above zero, and 13-20 uniques a day
on days with no commits and no CI runs is more likely automation than people:
Hypothesis.

- **T1 (high, Evidence): six P0 fixes are unreleased, not four.** 300.1 (issue
  #1's crash, closed on 2026-09-06 with the fix "on the next release"), 375.9,
  375.10, 376.2, 376.4 and 377.1 are all outside `v0.8.0`, which is 260 commits
  back. The published `bo-check-markup` still crashes on its documented usage
  (`npm pack @busy-office/ui@0.8.0`; ENOENT, exit 1). **377.5 is annotated with
  the true inventory**; the release stays the owner's.
- **T2 (medium, Evidence): the window since Slice 379 changed no shipped
  artefact.** 12 commits: 0 lines under `packages/` or `apps/docs`, 373 lines of
  loop tooling, 339 lines of markdown records. Since 0.8.0, framework source is
  1.3% of changed lines. This grill was armed by three slices that shipped
  nothing a consumer runs. **Filed as 381.2:** measure how often that happens
  before changing how rule 3 arms.
- **T3 (medium, Evidence): the last grill's framework-code command could not
  see JS.** It named `packages/core/src/behaviors`, which does not exist. With
  `-- packages/core/src` it lists `0ade896e` (context-menu.ts), which the report
  named by hand. **Corrected** in that report. **The command now sits in
  LOOPS.md §6's Exit**, the half of 377.7 this grill lands. This window reads **0
  files, +0 −0** (`git diff --numstat 2fad3cc7 HEAD -- packages/core/src`).
- **T4 (medium, Evidence): the last reading was never committed.** The four
  `adoption-*` rows sat uncommitted in `loop-metrics.jsonl`. **Committed with this
  slice.**
- **T5, T6, T7, T9 (low to medium): the channels need windows and a better
  mix.** The daily series quoted beside the weekly 16 covered a different
  window, and two of its zeros were npm ingestion lag. jsDelivr measures no use
  of the CSS or JS at all. Most weekly downloads are old versions. GitHub
  traffic was missing. **377.7 is annotated** with each.
- **T10 (low, Evidence): 377.7's structural half would wait behind about 20
  items.** **Landed here:** §6's Exit now requires the thesis section.
- **Refuted:** T8, "the one identifiable user is weaker than presented". The
  facts reproduce; the conclusion was judged a reframing, not a finding.

**The thesis answer, unchanged in substance.** Adoption still cannot be told
apart from zero on any channel that sees use. What this window adds is a
finding about the loop, not the market: for five hours it spent everything on
its own machinery, while six P0 fixes waited on a release. The question for the
owner is still 377.5 and 377.6. The question for the loop is 381.2.

## 5. Fed back to the Roadmap loop

Slice 381 records the corrections above and files two items: **381.1** (the
correction-site check's precision, self-test coverage and blind spots —
re-tune it or unwire it) and **381.2** (how often rule 3 arms on slices that
shipped nothing, measured before anything changes).
