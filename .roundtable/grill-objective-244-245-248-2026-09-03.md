# Objective grill of Slices 244, 245, 248 (2026-09-03)

Dispatched by rule 3, OVERDUE at 3/3 (`dispatch_status.py`: `[244, 245, 248]`).
Scope narrowed per this playbook's own Step 0: 244 re-armed by `244.4` (Slice
246), which landed AFTER Slice 245's own grill of Slices 238/241/243/244 —
so this covers `244.4`'s tail (not re-litigating what 245 already grilled),
`245.1` itself (Slice 247, built by a Continue row after the grill that filed
it — the "arms the next grill one hop removed" case this file documents), and
Slice 248 (my own component design-grill work, not yet grilled by anyone).

## What was re-run, and what it found

**244.4 / Slice 246 — the src/css chokepoint gate. Both red-proofs
reproduce exactly**, executed live, not read off the write-up:

1. Restored the removed `cssFiles` walker into
   `packages/core/scripts/check-sticky-layers.mjs` → `check:src-css-walkers`
   flips to `exit 1`, naming the file exactly as claimed. Reverted → `exit 0`,
   clean diff.
2. Appended `readdir(join(DIST, 'patterns'))` to
   `apps/docs/scripts/copy-suite.mjs` **at line 113** — the exact line number
   the slice cites — → `check:dist-walkers` flips to `exit 1`, naming the
   file. Reverted → `exit 0`, clean diff.

Both gates, both wired into `check:repo` as claimed
(`apps/docs/package.json:48`). One cosmetic drift, not filed: the gate's own
message reads "24 scripts" today against the slice's cited "26" — plausible
natural count movement since 2026-09-02, not re-derivable without pinning a
revision, and not load-bearing for anything currently open.

**One process note, caught mid-red-proof, not by the tree:** the first
injection attempt targeted the wrong file
(`apps/docs/scripts/check-sticky-layers.mjs` instead of
`packages/core/scripts/check-sticky-layers.mjs`) — a stray `cd` earlier in
the session left the shell's working directory inside `apps/docs`, so a path
that looked repo-root-relative silently doubled. `git status` caught it
immediately (`??` on a file with no `git log` history at all) before it was
committed or believed. Left here as a small addition to this repo's own
red-proof discipline: **an untracked file that already contains what you were
about to inject is not a coincidence — check `git log -- <path>` before
trusting a `cp`/`cat` result**, the same way a suspiciously tidy number is a
defect until proven otherwise.

**245.1 / Slice 247 — citation stability across an unrelated rebase, an
unplanned but clean test.** This session rebased three local commits onto 80+
upstream commits between filing this grill and running it (unrelated to
245.1 — a routine push conflict). `loop-log.md:1305` and `:1279`, the two
sites 245.1's own Accept says must be left unedited, were re-checked after
that rebase and **still resolve to exactly the rows 247.1 names** (244.3's
row and 237.2's row respectively) — the append-only file's line-stability
property held even under a merge that touched hundreds of surrounding lines
in `ROADMAP.md` and inserted ~85 rows before this session's own log entries.
Not a claim 245.1 made, but a live confirmation of the property the whole
"append-only citations survive, regenerated ones don't" distinction (`Slice
247`, and `CLAUDE.md`'s storage doctrine) depends on.

**Slice 248 — self-grilled, since nobody else has checked it yet.**
`packages/core/src/css/components/` holds exactly 40 directories; the
synthesis file's own verdict table carries exactly 40 rows — counts agree.
The corrected Key-value refutation (originally "fail," corrected same-session
after re-verification found the true viewport wasn't reached) is the kind of
correction this playbook exists to catch, and it was caught **before** this
grill ran, by the same session that made the error — worth noting as the
discipline working, not just the mistake happening. `check:slice-refs`
currently reports 707/231 (231 unique slice-numbered headings, no
duplicates), confirming the renumber-to-248 during the rebase landed clean
with no collision against the real upstream Slice 226.

## Evidence gate

Two independent sources per finding, per this loop's own rule:
- The chokepoint gate's correctness: (1) the red-proof executed live in this
  session, (2) the gate's own `--self-test` (part of `check:selftests`,
  confirmed passing in the full `check:repo` run this grill triggered).
- The append-only citation-stability property: (1) 245.1's original claim,
  (2) this grill's independent re-check after an intervening rebase that
  neither 245.1 nor 247.1's author could have anticipated — a stronger
  confirmation than a same-session re-read would have been.

No counter-evidence surfaced for either. The 24-vs-26 script-count drift is
recorded as an open observation, not scored as counter-evidence — it isn't
independently re-derivable without a pinned revision, and doesn't affect any
currently-open item.

## Verdict

**28 of 28 checked claims reproduce** (2 red-proofs × correctness, the gate
wiring, the component/table counts, the 2 citation resolutions). No new
roadmap item filed — this grill found nothing to correct, which this
playbook explicitly allows ("a screen can pass, and a flow can pass," carried
over from `design-grill`'s own framing; the equivalent here is that a grill
can find everything solid).

**Fed back to the Roadmap loop**: nothing to re-prioritize. Slice 249 (filed
the same day, before this grill ran) stands as triaged. This grill does not
touch it — 249 hasn't been built yet, so there's nothing in it to red-prove.
