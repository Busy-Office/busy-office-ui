# Resume state — read this at Step 0 of every wake

> **⚠ ALSO READ `.roundtable/ENVIRONMENT.md` — the git/build traps and the
> toolchain that works.** It used to live in this file. It does not any more
> (roadmap 169.3, 2026-08-28), because this file is rewritten wholesale every
> wake and that is where corrections go to die. `LOOPS.md` Step 0 names both
> files, and **three** advisory checks run from `record_iteration.py` — the
> charter check, `check:resume-slice-ids`, and `polish_requeue.py
> --verify-stamps`. All three REPORT; none fails a build (roadmap 175.3). Run
> them against the file as it now stands rather than trusting a stale reading.

The wake prompt says *"don't assume prior-turn state"*. This file is how a wake
picks up work that was left mid-flight, so the instruction stays true across a
context clear. **Keep it current whenever a slice is left uncommitted, and empty
it the moment the slice lands.**

**Citation practice for this file: cite by slice number only, never by raw
`ROADMAP.md:NN`.** A slice number survives every rewrite; a line number
survives none.

---

## In flight: nothing

Last updated 2026-09-09 (**cloud** wake, scheduled routine). Working tree clean
at hand-off apart from this file, `ENVIRONMENT.md` (§4b is new), `loop-log.md`,
`loop-metrics.jsonl` and `STATUS.md` (`INDEX.md` regenerated byte-identical).
**No collision this wake** — `origin/main` read `c7c22ce9` at Step 0 and again at
the mandated pre-commit fetch; it was the local tip both times.

**Reconcile this file against `ROADMAP.md` before trusting its open set:**

```
grep -cE '^\s*[0-9]+\. \[ \]' ROADMAP.md          # open-checkbox count
node apps/docs/scripts/check-resume-slice-ids.mjs # names any stale closed ids
python3 scripts/loops/roadmap_scope.py            # OPEN set + sweep scope
```

**Every closed id this file names is named as history or as precedent, never as
open work:** `339.2` (the item THIS wake closed), `339.1`/`341.1`/`326.3` (the
precedents re-measured or cited here), `351.1`, `324.3`, `297.1`, `332.1`,
`355.3`, `359.4`, `310.1`, `298.1`, `316.1`, `338.1`, `312.2`. **`350.1` and
`345.1` are named repeatedly and are OPEN — deliberately so.** Nothing here
claims an open item that is not.

## ⚠ WHICH RULE FIRES NEXT — rule 3, Objective, **OVERDUE at 3/3**

`dispatch_status.py`, read immediately after this wake's recording (`LOOPS.md`
asks for exactly that comparison; it has found two of the five parser bugs):

```
Standardize   1 / 4 Continue round   ok
Objective     3 / 3 slices           OVERDUE   [338, 370, 371]
Optimize      0 wake-date(s) newer   ok
```

**Re-run it** — a collision could land a row between this line and your wake.

Rule 1 has no open P0 (**0** across 23 open items). **Rule 2 is at 1/4**, armed
by this wake's Continue row. **Rule 3 is OVERDUE and fires next**, so rules 4-8
should not be reached; the grill's material is Slices 338, 370 and 371.

**The previous hand-off predicted exactly this and was right** — it wrote *"a
wake that dispatches rule 4 and lands an item should expect rule 3 to fire the
wake after."* Recorded because the prediction was made and HELD, which is the
counterpart of last wake's entry recording one that did not.

**The arming label `[338, 370, 371]` mixes kinds, and the third entry confirms
last wake's diagnosis rather than a parser bug.** `338` is an ITEM id resolving
to slice 369; `370` and `371` are genuine **slice numbers**. Last wake concluded
the label *"is not inherently unreliable — it reflects how the row was
written"*, and this wake **tested that by writing its `--item` text to begin
`Slice 371 —`**; it resolved correctly. That is a deliberate confirmation, not a
coincidence. Still **no parser item is filed** (eighth of its kind, refused on
`355.3`, `359.4` and `LOOPS.md`'s own conclusion). Resolve any label you did not
write by reading the commit subject at the row's sha; never treat it as an
ordinal.

## What landed: Slice 371 — `339.2` decided, by splitting it

**Dispatched by rule 4.** Rules 1-3 were evaluated and none matched (0 P0;
Standardize 0/4; Objective 2/3 at Step 0b). Step 1 read both intakes with §8's
controls — issues **1**, discussions **0**, `/not-a-real-route` **404** — and
triaged nothing. Issue #2's `updated_at` is unchanged at 2026-09-06, a **fifth**
consecutive hand-off.

**Rule 4's target was re-derived, not carried.** The **12** open items older
than `339.2` are Slice 15, `112.3`, `112.4`, `249.7`/`.10`/`.11`/`.12`/`.13`,
`273.2`, `296.3` (owner-blocked); `320.3` (browser-blocked, screenshot sense);
`335.1` (cloud-blocked, WRITE sense). 12 older + `339.2` + 11 newer = the 24
counted at Step 0.

### The Accept's first clause was "re-measure the series", and the instrument that produced the premise no longer exists

Measured by **importing** `report_loop_prose.dispatch_sections` rather than
re-implementing its convention, asserting **exactly one** rule-3 match per
revision, over the **25** revisions of `LOOPS.md` in `8848ed55^..HEAD`:

```
8848ed55  2026-09-05    672
632bfc46  2026-09-05    975   +303   Slice 279.4
… 23 revisions …        975     +0   through 5ce62916
```

**Shape reproduces exactly; both endpoints read 12 words above the filed
660 → 963.** That gap was run down rather than rounded off: **12 is exactly the
rule's own heading line** (12 words, identical at both endpoints), which
`dispatch_sections` counts in a rule body and 339's ad-hoc method excluded — and
**it could not have used that function, because the function did not exist**:
`git log -S "def dispatch_sections"` puts its first appearance at `1310b81a`
(Slice 353), **one day after `339.2` was written**. The delta **+303 is identical
under both conventions**, so the premise stands.

**Reconciled a second, independent way** — at paragraph granularity the change is
`OLD P3 (146w) → NEW P3 (89w) + P4 (160w) + P5 (200w)` = 449w, and
**449 − 146 = 303**.

### The verdict: three kinds, where the Accept anticipated two

- **P3, 89w — INSTRUCTION.** The operative loop set and its exclusions. Untouched.
- **P4, 160w — FORENSICS, duplicated in full.** All six figures verified present
  beside `CLOSES_A_SLICE` in `dispatch_status.py`, by line range, including the
  flat-average caution that reads as instruction. The module also carries three
  things the prose never did (reconciliation against live output, the
  circularity check, the `6 → 15` figure), so it is the **richer** home.
- **P5 — INSTRUCTION.** 279.4's loop-SET argument. Untouched.

**What decided it, and it is the finding worth carrying:** `339.2` filed rather
than cut because *"279.4 argues explicitly for its inline placement"*. Checked
against the text — **all three fragments it quotes as that argument are in P5,
and not one is in P4.** The cost narrative was never covered by the argument
protecting the lesson, so P4 moved and P5 stayed uncut, **overriding nothing**.

**Verified structurally, not by reading the diff:** the moved text is
byte-identical in `LOOPS-archive.md`, **exactly one copy**, absent from
`LOOPS.md`; P3 and P5 assert byte-identical to HEAD. Rule 3 **975 → 907**;
dispatch region **7,552 → 7,484**, a delta equal to the rule delta, which is the
check that nothing else moved. HEAD's 7,552 reproduces `report_loop_prose`'s own
published figure, so both readings are on the same instrument.

## One defect in this wake's own work, caught before publishing

**A wrong count in the slice's own prose.** It read *"the eight open items older
than `339.2`"* while the enumeration in the same sentence listed **twelve**. The
by-hand verifier pass caught it by re-counting the enumeration beside the
number; corrected to 12 and reconciled to the 24 total before committing. This
is CLAUDE.md's "a number you report is load-bearing" in its smallest form, and
the number had already been written.

**A second, smaller one that was NOT a defect in the work:** the probe verifying
the published `dispatch_status.py` line citations reported 6 of 11 MISS. The
citations were right; the **probe** was wrong — a fixed-string search across a
wrapped comment line. Re-run per range, all nine ranges verify. *An instrument's
first output is not evidence*, arriving inside the check written to protect a
claim.

## NOT VERIFIED, said plainly — and the visual debt is unchanged

**No 1440/390 light-and-dark screenshots — a cloud wake has no Podman.** This
wake owes none, and that is **structural rather than a judgement**: `git diff
--stat` was read, and the slice diff is `LOOPS.md`, `LOOPS-archive.md` and
`ROADMAP.md` only. **No CSS rule, no docs page, no `.astro` file, no generated
artefact, no shipped JS.**

**The eight older debts are unchanged and unspent**, counted from the previous
hand-off's enumeration rather than carried as a number: Slice 352's two
(`/components/data-table`'s performance table and `/concepts/scale`'s scaling
table, both at 1440 and 390 in both themes); Slice 345's two
(`/patterns/output-form` **in print** and the RF tile grid on
`/patterns/rf/rf-landing-rf/` at both widths); and the four older —
`292.4/292.5`'s screenshot lane on `/components/icon`; Slice 319's paragraph on
`/patterns/kanban` at 390px; `320.3`'s `ApiTable.astro` `0.5rem` against
`ClassRef.astro` `.4rem`; and Slice `310.1`'s three `prod/` Refresh buttons.

**Gates.** This container arrived with **no `node_modules`** (`npm ci` was
needed — `ENVIRONMENT.md` line 612 says so, and the first `build` failed
`code 127` on a missing `stylelint` before it was run). The seven the wake
prompt mandates were then run, every one green, figures read off their own
output: core `build` (incl. `check:package` **185** files), core `test`
(**165** tests / 29 files), `docs:build` (`slice-refs` **1029** assertions /
**353** slice sections, `vendor-names` **623** files), `check:claims`
(**176** live, **3 NOT VERIFIED**, which is `ENVIRONMENT.md` §6b's container
fact, not a regression), `test:axe` (**128** pages × 2 widths, **zero**
violations), `check:layout` (**128** pages), `check:repo`.

**Said precisely: the full 17-entry CI set was NOT run this wake.** The prompt
named seven and those seven ran; the other ten (`lint:css`, `check:formatting`,
`check:scroll`, `check:forced-colors`, `check:target-size`, `check:search`,
`check:pseudo`, `check:quickstart`, `check:po-app`, `npm run suite`) were not,
and CI will be the first to run them on this push. The diff is three markdown
files, none of which those ten read, but that is a **reason to expect green, not
a substitute for having run them** — the distinction is stated rather than
blurred, per this repo's own rule that a gate which cannot run must say so.

`docs:build` was re-run to exit 0 after the last `ROADMAP.md` edit and again
after this file was written, per `ENVIRONMENT.md` §3b, before the push. It gates
`.roundtable/**` and `ROADMAP.md` content.

**The verifier agent was not used** (this session's standing instruction is not
to spawn agents unasked), so `LOOPS.md` §2 step 6's verifier pass was done by
hand: the staged diff re-read adversarially before committing. **It is what
caught the "eight"/twelve count above.**

## The metric recorded, and the reason for each candidate not recorded

- **`dispatch-region-words` = 7,484 words — RECORDED.** `LOOPS.md` **was**
  touched this wake and the region genuinely moved (7,552 → 7,484). It is one of
  the 8 names rule 5 can pair across days, so the sample is an input rather than
  an addition to the 39-name single-day pile.
- **`gates`** — unchanged at **56**; this wake added no gate. A same-value
  sample moves nothing.
- **`claims`** — read **176** live, identical to the sample already in the pair.
- **`axe-violations`** — 0 again; the line already marks it `NEVER MOVED`,
  because `test:axe` fails above 0.
- **Rule 3's 907-word figure was NOT recorded as a metric**, and that is a
  choice: it is a section of a file whose region-level metric is already
  sampled, and a second name measuring a subset of the first would double-count
  the same movement.

## The open set is 23 — no P0

`roadmap_scope.py` at the slice commit and the raw checkbox count agree at
**23**. Slice 371 closed one item (`339.2`) and opened **none**, so the set is
−1 on the previous hand-off's 24.

- **cloud-takeable: 10** — `345.1`, `346.1`, `348.1`, `349.1`, `350.1`,
  `352.1`, `352.2`, `353.2`, `362.1`, `369.2`. **`345.1` is now the oldest of
  these** and is what rule 4 would dispatch — but **rule 3 is OVERDUE and sits
  above it**, so expect the next wake to grill rather than build. `362.1`
  carries Slice 364's amendment on the `include`.
- **cloud-blocked in the WRITE sense (1):** `335.1` — the Discussions intake
  needs a GraphQL `createDiscussion`, and the **403** was re-confirmed by
  measurement in Slice 366. A local wake can take it. **Not re-derived this
  wake** — only its position in the age order was.
- **owner-blocked (11):** Slice 15 (AT runtime evidence, owner hardware),
  `112.3` (**BLOCKED ON OWNER BRIEFS**), `112.4` (blocked on 112.3's verdict),
  `249.7` (holds its remaining rows for `249.10`), `249.10`, `249.11`,
  `249.12`, `249.13`, `273.2` (**OWNER CALL**), `296.3` (**OWNER CALL**),
  `369.1` (a palette-wide print behaviour change with a visible result).
- **browser-blocked in the SCREENSHOT sense (1):** `320.3`.

10 + 1 + 11 + 1 = 23, asserted rather than left to the reader. **The fifth kind,
`artifact-lost`, is empty.**

## The archive sweep: 60.9% — unchanged in direction from last wake's rise

```
python3 scripts/loops/roadmap_scope.py
  8669 / 14230 = 60.9%    (previous wake's tip)
```

**Re-run this at your Step 0** — this wake's slice ADDS ~150 lines of open-side
body to `ROADMAP.md` while closing one item, so the two halves of 208.1's ratio
move in opposite directions and the figure above is the *pre-slice* reading,
deliberately not re-quoted post-hoc from a stale run.

**Not dispatched by this wake, and the reason is the dispatch**: rule 4 fired on
an item, and a sweep is a hand-checked bulk edit one slice at a time
(CLAUDE.md). **`249.12` is named again** — the open **OWNER OR ARCHITECTURE
CALL** on the archival trigger. **10 targets are NAMED by a still-open item**
(`roadmap_scope.py` lists them) and must be read before moving (236.2).

## The standing environment fact: CI HAS NO `paths-ignore`

`312.2` removed it entirely. **A push that touches only `.roundtable/**` runs
the full suite.** The consequence a wake feels directly is `ENVIRONMENT.md` §3b
— *re-run `npm run docs:build` after writing this file, before pushing* — and it
was executed this wake, after this file was written, before the push.

## CHECK CI AFTER PUSHING — and filter the run list yourself

Use the plain listing and match the sha in your own code; **never `?head_sha=`
with an abbreviated sha**, and **take the full sha from `git rev-parse HEAD`,
never by extending a short one already on screen** (`ENVIRONMENT.md` §6d):

```
curl -sS -H "Authorization: bearer $GITHUB_TOKEN" \
  "https://api.github.com/repos/Busy-Office/busy-office-ui/actions/runs?branch=main&per_page=6"
```

## Step 0 traps

**Trap 1 bit.** `git branch --show-current` answered **EMPTY** at Step 0 — the
container arrived detached at `c7c22ce9` — and was fixed with
`git fetch origin main && git checkout -B main origin/main` before any commit;
re-read as `main` before committing.

**Trap 2 bit; trap 2b did NOT.** The clone arrived shallow (`true`, 50 commits);
`git fetch --unshallow origin` completed inside the timeout, giving **2,110**
commits, and left no `shallow.lock`. Per §2 the tag count is the check, not a
pinned value — this container's `--unshallow` again brought them (**8**).

**A third, and it is in `ENVIRONMENT.md` but not among the numbered traps: no
`node_modules`.** `npm run build -w @busy-office/ui` fails `code 127` on a
missing `stylelint` binary, which reads like a toolchain break rather than a
missing install. `npm ci` first. **No item is filed** — line 612 already says
*"no node_modules at start"*; what cost time was that the failure names
`stylelint`, not the cause.

**A FOURTH trap was found this wake and is now `ENVIRONMENT.md` §4b: amending
the slice commit AFTER `record_iteration.py` strands the sha in every row it
just wrote.** The amend rewrote `5571294d` → `4e6b83c1`, and three rows (one
Continue + two `--also-refused`) kept pointing at an object that exists only in
this container's reflog. **`git cat-file -e` still answers *yes* locally**, so
the obvious check passes while the published record is already broken — this is
the mechanism behind `LOOPS.md` §0c's *"five whose sha no longer exists, rebased
away"*, which records the symptom but not the cause. Corrected before pushing
(count asserted at 3 first, mirrors rebuilt, reconciled **1710 = 1710**). **No
item filed** — it is a sequencing rule, not something a gate can see.

**No `git worktree` and no `git stash` were used this wake.** The probes (the
series, the paragraph arithmetic, the citation verification) lived in the
scratchpad and never in the repo.

## Direction

Nothing new from the owner reached this wake to triage. **Both intakes were
read** (issues **1** open, discussions **0** open).

**Five things want the owner's attention. The first two are unchanged and are
the two biggest.**

1. **`369.1` — printing from the dark theme puts 19,511 of 26,817 painted text
   fills below AA on paper, across 125 of 128 pages.** Chrome's economy mode is
   what keeps that from being worse, and it is a **UA behaviour no other engine
   is known to share**, so the reading is a floor for Chrome and says nothing
   about the others. The proportionate fix is one `@media print` block
   re-pointing the theme tokens at their light values — a deliberate exception
   to `check:print-tokens`'s own rule, and so the owner's to make.
2. **Issue #2 is open and carries only the triage comment**, with `updated_at`
   unchanged for a **fifth** consecutive hand-off. Slice 317 refuses the
   component with the measurement; Slice 319 corrected a second false claim on
   the page the reporter was pointing at. **Replying and closing is an owner
   action.** Whether a *wake* should post that comment was `297.1`, closed by
   Slice 335 — read it before re-raising.
3. **`249.12`** — the stated-trigger question for the archive sweep, an explicit
   **OWNER OR ARCHITECTURE CALL**. The last two wakes measured the share moving
   in **both** directions with nothing archived, which is the argument that a
   percentage trigger fires for reasons unrelated to how much closed history the
   file carries. **This wake adds a third shape**: a slice that closes one item
   and adds a long entry moves the ratio's two halves in opposite directions at
   once, so even the SIGN of the move is not predictable from the work done.
4. **`273.2`** — whether a Polish round whose score does not move should
   increment `dry`. Not touched this wake; rule 6 was never reached.
5. **`335.1` cannot be settled by any cloud wake.** A local wake can do it in
   one command, or the owner can file a throwaway Q&A discussion and let the
   next wake read it.

**A sixth, carried forward unchanged: `362.1` will change published sample
code.** Adopting `astro check` means resolving 22 DOM-narrowing errors inside
inline `<script>` blocks that readers copy off pattern pages.

**`ENVIRONMENT.md` WAS touched this wake** — §4b above is new. The standing note
otherwise holds:
the file has no size discipline and no open item asking for one (`332.1` closed
on the finding that it is long because the environment is hostile). **If the
owner wants that discipline it needs filing as its own item.**

**Nothing this wake did is outward-facing or hard to reverse.** `CLAUDE.md` is
**byte-for-byte unchanged**. `LOOPS.md` changed — the dispatch region a wake
reads every wake is **7,552 → 7,484 words**. `report_loop_prose`'s ratchet line
was re-read after the slice commit and now reads `LOOPS.md 0 up — last cut
4e6b83c1 (2026-09-09)`, so the run **counts this as a cut** and the previous one
was `f9e0f17d` (2026-09-07). Whole-file: 18,309 → **18,241**, the same −68.

**The two files together GREW, and the hand-off says so rather than quoting
only the cut.** `LOOPS-archive.md` went 3,622 → **3,880 (+258)**: 160 is the
moved paragraph, **98 is prose written this wake to explain the move**, so the
pair is **+190 net**. That is 339.1's own recorded failure recurring — *"the
narrative that came out was replaced by a paragraph explaining why it came
out"* — caught here by measuring the ARCHIVE and not only the file being cut.
The explanation's first draft was **167** words and was tightened to 98 against
that finding. **A wake that cuts a section should measure both ends**; the win
is that the region read every wake is 68 smaller, not that there is less prose.
