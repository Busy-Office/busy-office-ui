# Objective grill of 406.6 (full), with 406.1, 377.11, 377.12 and Slice 407 narrowed (2026-09-26)

Dispatched by rule 3 at `Objective 3 / 3 slices OVERDUE [377, 406, 407]`. The
last reset was Slice 406 (`5773572c`). Five agents ran in one workflow
(`wf_4ffec604-a14`), read-only on the repo:
- a defect hunter on 406.6;
- a refute-first verifier that could consult Jev AFTER each verdict, the
  first run under CLAUDE.md's "Agents consult it too" rule;
- two narrowed reproducers;
- a completeness critic.

The critic named no blocking gap. Raw returns: session scratchpad
`grill-408/result.json`.

## 1. Scope

| label | resolves to | shipped lines | grilled |
|---|---|---|---|
| 406 | 406.6 (P0), the grid focus ring (`c8ec19c2`) | 20+ / 1− in `data-table.css` | **full**: the only framework code since the last grill, and a shipped accessibility fix |
| 406 | 406.1 (P0), snapshots never nest (`45a0bfa7`) | 0 | narrowed |
| 377 | 377.11, the combobox commit equality (`00b8511f`) | 0 | narrowed |
| 377 | 377.12, the container's build-id (`86d34785`) | 0 | narrowed |
| 407 | the Standardize sweep (`1a64e98b`) | 0 | narrowed |

All five were built this evening by the dispatcher, and none had been
grilled before.

## 2. Claims reproduced

42 claims were re-run by their own commands: **30 yes, 10 partly, 2 no**.

| group | claims | yes | partly | no |
|---|---|---|---|---|
| 406.6 (full) | 13 | 11 | 1 | 1 |
| 406.1 and 377.12 (narrowed) | 15 | 7 | 7 | 1 |
| 377.11 and Slice 407 (narrowed) | 14 | 12 | 2 | 0 |

**Both "no" claims are the dispatcher's, and so are most of the "partly":**
- **406.6, "Accept clause 2 is met by removal; the ring's grounds were
  already in `RING_GROUNDS`."** No: a filled `<a class="bo-btn">` or
  `[tabindex]` widget in a grid still paints on its accent fill. The fix also
  moves control rings onto row-state and tone tints (`state-error-bg`,
  `warning-subtle`, `success-subtle`), which `RING_GROUNDS` does not measure
  (minimum 3.01, forest-light error row).
- **377.12, "the stamp refuses unless the container passes `BUILD_SHA` and
  `BUILD_DIRTY_PATHS`."** No: only the sha is enforced. The Containerfile's
  `ARG BUILD_DIRTY_PATHS=""` makes an omitted arg read as a clean tree, so
  CI's form (`BUILD_SHA` alone), run by hand on a dirty tree, stamps it
  clean.
- **406.6's geometry:** the minimum spare is **1.0px at the auto tier**,
  the density the demo itself renders at 390px, not 1.5px at compact. Still
  0 of 460 readings cross a cell edge.
- **406.1, "counts the expected switchers from the committed pages":** it
  does, but `dist/v` is a verbatim copy of `versions/` made one line earlier,
  so on a nested tree the two counts agree by construction: 375 rewritten,
  exit 0, with 103 nested pages claiming the wrong version. `badSnapshots`
  is the only nesting detector.
- **377.12's other claims:**
  - **HEAD:** the served stamp names `3a50f171`, not HEAD `24927cd2`. The
    two later commits touch no build input, so the content matches.
  - **The self-test:** it cannot fail on the `--from` lookahead.
  - **`copyInputs`:** it reads single-line shell-form COPYs only.
  - **The stamp's git-branch parser:** it mis-reads quoted special names. No
    tracked file has one today.
  - **Gitignored paths:** seven sit under the COPY inputs with no
    `.containerignore`, so they enter the image unseen by `dirtyPaths`.
- **377.11:** `detail.value` is never asserted against the option's
  `data-value`. The palette check compares the page's echo of `detail.value`
  with `detail.value` itself, which is self-referential. A wrong value
  passes all 5 rows.
- **Slice 407, "archive sweep not due":** no written trigger exists to be
  false against (249.12 is open), so it is a judgement, not a measurement.

## 3. Findings on 406.6, after refute-first verification

**None is a P0. 5 are confirmed, 3 narrowed and 0 refuted.** The Jev reading
is the verifier's, taken after its own verdict.

| id | verdict | severity | Jev (model) | what |
|---|---|---|---|---|
| F1 | narrowed | defect | 0.91 (`typesafe/jev-1.13-20260917`) | **Placement keys on element names, not on fill.** A focused `<a class="bo-btn">` or `[role=button][tabindex].bo-btn` in a grid keeps the inset ring on its accent-solid fill: 1.25–2.96:1 in 14 of 14, the P0's own failure class. Narrowed because no built page composes it, and it is not a regression: the pre-fix rule inset everything. |
| F2 | confirmed | minor | 0.81 (`jev-1.13.0`) | The case accepts offset 0, the merged "bigger checkbox" the fix exists to avoid. |
| F3 | confirmed | minor | 0.85 (`typesafe/…`) | Nothing gates "stays inside the cell". An offset that crosses into the neighbour passes the case. |
| F4 | narrowed | minor | 0.88 (`jev-1.13.0`) | At the auto tier, a first-column control's ring covers 2 of the 3px row-state bar, the row state's one non-colour channel. Ring against bar reads 1.02–3.53, 26 of 28 under 3. The ring itself stays visible. |
| F5 | confirmed | doc-claim | 0.79 (`jev-1.13.0`) | "Met by removal; grounds already in `RING_GROUNDS`" does not hold (§2). |
| F6 | confirmed | doc-claim | 0.89 (`typesafe/…`) | The geometry names the wrong tier (§2). |
| F7 | narrowed | nit | 0.87 (`jev-1.13.0`) | The published-debt match ignores the ground. |
| F8 | confirmed | nit | 0.83 (`jev-1.13.0`) | The case never asserts that the theme applied. |

**Jev, surfaced and not settled:** five "confirmed" verdicts (F2 0.81, F5
0.79, F8 0.83, the geometry 0.82, Accept clause 2 0.77) have Jev in the
unverified band. Each rests on a reproduced measurement. The model string
alternated between two values within one agent's run, which is more data for
377.10.

## 4. What this grill does not cover

- **A second browser.** 406.6's checkbox is native (`accent-color`), and
  Firefox and Safari paint it differently. That is 375.11's standing
  NEEDS-RUNTIME, since Firefox has not been opened.
- **Forced colours for the fixed controls** were judged from screenshots, not
  pixels.
- **406.1 through the real cut script**, which writes to `versions/` and
  belongs to the next release.
- **377.12 live on a dirty tree.** The grill saw only a clean stamp.
- **A brand-token change that pushes a grid ring under 3:1, end to end.**
- **Refute-first verification of the narrowed repros' findings.** They go to
  triage as found.

## 5. Thesis section

- **Adoption**: quoted from `python3 scripts/loops/record_metric.py
  --adoption`, taken 2026-09-26 18:17, 20 metrics recorded and 0 channels
  unreadable.
  - All 20 are identical to the 16:32 reading (compared row by row from
    `loop-metrics.jsonl`): npm `@busy-office/ui` 989 in the window
    2026-08-26..09-24, 134 outside publish days; `create-ui` 650 and 83;
    jsDelivr 30 hits, 0 to `dist/`; GitHub stars, forks, watchers,
    non-owner issues and PRs, and discussions all 0; 20 views and 367 clones
    over 14 days.
  - 0.9.0 still cannot show: it was published after npm's window ends.
  - What no channel sees: docs readership, private proxies, the CI and
    mirror share of npm, traffic beyond 14 days, and copied `dist/`.
- **First user**: busy-office-erp has not moved on the UI.
  `git log --all --since=2026-09-10` now counts 2 commits (Slice 406 read 3;
  one workflow worktree commit is gone). `main` is still `e7504e2` (15:10,
  ADR-0020, a process change), and ADR-0016 is still "Proposed".
- **Comparators**: re-read with `npm view <pkg> version time.modified`:
  fundamental-styles 0.41.9 (09-17), UI5 Web Components 2.27.2 (09-25),
  PatternFly 6.6.1 (09-25), Carbon `@carbon/styles` 1.116.0 (09-23). All are
  unchanged since Slice 406, so its comparison stands: none of the four
  ships CSS, delegated behaviour and an htmx story in one package, and the
  floors to beat remain `prefers-contrast` themes, five row sizes and a
  weekly release cadence.
- **Framework code**: `git diff --numstat 5773572c HEAD -- packages/core/src`
  reads **1 file, +20 −1**, `components/data-table/data-table.css`, over 10
  commits. It is 406.6's fix, the first framework change since Slice 403's
  window, and it is the subject of §3.

## 5a. Jev J2 on this report's exit (advisory)

My verdict came first: exit met. Jev read **0.81** (`typesafe-ai/jev`) on
the verbatim thesis and triage sections, in the unverified band. The one gap
it could fairly see was that the comparators bullet said "unchanged" without
restating the comparison it supports. That line was added. Jev was not
re-asked: a second call on nearly the same state would be fishing for a
number.

## 6. Round-table (Hypothesis: one source, this reading)

- **Accessibility reviewer:** the fix was right for the controls it named
  and wrong in its own description. "Placement follows fill" is the correct
  shape; keying it on four element names was not that shape, and the grill
  found the gap in one pass. The next version should name the fill-less
  elements (cells, cell links) and move everything else outward.
- **Maintainer:** the same pattern as Slices 403 and 406, at a smaller
  scale. Every instrument built this evening (the snapshot count, the stamp,
  the equality predicate) passed its own red-proof and still has a hole its
  red-proof did not exercise. The grill is where those holes are found.
- **Adoption skeptic:** the first framework change in three grills is a
  focus-ring offset, and adoption did not move. Nothing here changes the
  thesis reading.

## 7. Triage

- **Filed as Slice 408:**
  - 408.1, grid ring placement by fill, not element name (F1), plus the
    case's gaps (F2, F3, F7, F8) and the row-state bar (F4);
  - 408.2, the container stamp's dirty half is enforced, and gitignored
    build inputs become visible (377.12's "no" and partlys);
  - 408.3, the combobox case asserts `detail.value` against the pressed
    option's `data-value` (377.11's partly).
- **Corrected in place:**
  - 406.6's DONE note and CHANGELOG entry: the geometry tier, clause 2, and
    the scope "input, select, textarea or button";
  - 406.1's DONE note: the count agrees by construction, and
    `badSnapshots` is the only nesting detector;
  - 377.12's DONE note: only the sha is enforced;
  - 377.11's DONE note: only the text half is asserted;
  - Slice 407: "not due" is a judgement with no written trigger.
