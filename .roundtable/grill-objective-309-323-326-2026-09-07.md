# Objective grill — Slices 309, 323, 326 (2026-09-07, cloud wake)

Dispatched by **rule 3** at `Objective 4 / 3 OVERDUE [307, 309, 323, 326]`.
Rule 1: **0** open P0. Rule 2: `Standardize 0 / 4 ok`.

**Scope (§6 step 0).** **307 dropped** — grilled in full by Slice 309, which is
itself in the arming set. Grilled: **309, 323, 326**. A grill inside the arming
set is legitimate; its published findings are claims like any other, and 309 is
where the one false assertion turned out to be.

**Verdict: 74 of 75 re-derived assertions reproduce.** Slice 326 clean at 49 of
49, Slice 323 clean at 9 of 9, Slice 309 **16 of 17**. Both filed defects sit in
prose BESIDE a measurement that is itself correct — roadmap **192.1**'s class,
for the fourth grill running.

Every figure below was produced by the command beside it in this container
(Intel Xeon @ 2.80GHz, linux x64, Node v22.22.2), not read off the slice.

---

## Slice 326 — 49 of 49

### Lane 3 (`326.1`)

```
npm run report:prose -w docs
#  118 documentation page(s) of 127 built · median 792 · mean 953 · total 112,395 words
#  over 2x the median (1584) — 10 page(s)
#  by family, over 2x — base 1 · components 3 · concepts 5 · patterns 2 = 11
#  union of the two = 15
```

All five published totals, the threshold, both flag counts and the union
reproduce exactly.

**The 16-set was rebuilt from the three ids the fixed clause names**, which is
the operation the clause asks a wake to perform:

```
awk 'NR>=17549 && NR<=17800' ROADMAP-archive.md \
  | grep -oE '^\s+[0-9]+\. `/[a-z-]+/[a-z-]*/?`'      # 158.1's twelve — all twelve resolve
```

`158.1` → data-table, richtext, which-pattern, form, editable-grid, list-report,
calendar, money, combobox, tabs, layouts, **output-form**; `161.1` → motion,
js-behaviors, design-language; `178.3` → scale. **15 of 15 flagged pages are
members**, `output-form` is the sixteenth and is no longer flagged. So the clause
is executable as written and lane 3 is genuinely clean.

**Commit forensics reproduce to the second:**

```
git log -S'Verdicts to date' --date=format:'%Y-%m-%d %H:%M:%S %z' -- LOOPS.md
#  87bf0f54  2026-08-28 07:42:07 +0000   the clause is written
#  e1f5a12f  2026-09-07 10:46:56 +0000   Slice 326's own fix (was ONE commit before it)
git log -1 --date=iso-strict-local e5edf61f     # 2026-08-28T19:46:14+00:00
git merge-base --is-ancestor 87bf0f54 e5edf61f  # true
```

**The negative controls — see `327.2`.** All seven reproduce at `7c6f7ecd` and
none at `e1f5a12f`; the table is in ROADMAP `327.2`.

### Lanes 1, 2, 4 (`326.2`)

```
export CHROME_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
npm run scan:dead-style -w docs
#  0 dead style attribute(s) on 0 page(s); 1272 live inline style attribute(s)
#  (1677 declaration(s); 273 attribute(s) carry more than one)

npm run report:css-repeats -w @busy-office/ui
#  74 source file(s) · 242 rule(s) with 3+ declarations · 230 distinct bodies · 8 repeated
#  x2 ×5, x3 ×2, x4 ×1;  the x4 group = money.css ×2 + quantity.css ×2  (two components)

python3 scripts/loops/report_loop_prose.py
#  ratchet: CLAUDE.md 33 up / never cut · DESIGN.md 23 up / never cut
#           LOOPS.md 14 up, last cut 8848ed55 (2026-09-05)   ← 13 at 326's own reading, +1 for its commit
#  by region: dispatch 1,525 -> 6,759  +343.2%
```

**The per-section table was re-derived with a splitter written for this grill**,
not by re-running 326's:

```
git show <rev>:LOOPS.md | awk '/^## Playbooks/{exit} {print}'   # then split on ^#{2,4} and ^[0-9]+\. \*\*
```

| section | `8848ed55` | `25b9fdd3` | `e1f5a12f` | published |
|---|---|---|---|---|
| `Step 1` | 300 | 671 | 671 | 300→671 ✓ |
| rule 3 | 660 | 963 | 963 | 660→963 ✓ |
| rule 5 | 349 | 573 | 573 | 349→573 ✓ |
| `Step 0` | 563 | 714 | 714 | 563→714 ✓ |
| loops table | 214 | 266 | 266 | 214→266 ✓ |
| **`Step 0c` (the cut)** | **936** | **936** | **936** | flat ✓ |
| region total | 5,658 | 6,759 | **6,759** | 6,759, +1,101 ✓ |

Two things worth keeping:

- **`8848ed55` IS 274.2's cut** (`git log -1 8848ed55` — *"the dispatch region's
  remaining slab is in Step 0c"*), so Slice 308's table and Slice 326's share a
  base. Their identical `was` columns looked like the *identical-value-across-
  inputs* tell and are not: the sections did not move between the two readings.
- **The region figure survives its own commit**, which `ENVIRONMENT.md`'s figure
  rule predicts it would not: 6,759 at both `25b9fdd3` (the parent) and
  `e1f5a12f`. Slice 326's `LOOPS.md` edit landed in the Standardize playbook,
  below `## Playbooks`, so it is outside the region. Checked rather than assumed.

Trace figures: `git rev-list --count 7c6f7ecd` → **2,011** ✓ (2,012 at
`e1f5a12f`, 2,013 at HEAD); `git tag | wc -l` → **8** ✓; open P0 **0** ✓; issue
#2 `updated_at` **2026-09-06T15:10:34Z**, unmoved ✓.

---

## Slice 323 — 9 of 9

The commit-granularity replay, run from the script's own header against the
slice's own commit:

```
python3 <replay.py> a6e7fff6
#  958 revisions -> {'STALE': 581, 'SKEW': 51, 'ok': 323, 'NO LIVE INPUT': 2, 'no metrics': 1}
#  SKEW occasions: 7
#    2026-08-13 bundle-gz-kb 8 · 2026-08-15 bundle-gz-kb 10 · 2026-08-16 bundle-gz-kb 4
#    2026-08-17 ci-gates 1 · 2026-08-18 ci-wall-time 4 · 2026-08-19 framework_classes 23
#    2026-09-06 axe-violations 1   ← Slice 306's own
```

Exact, including the seven occasions and their metric names. At HEAD it reads
962 revisions / 585 STALE with the SKEW and ok columns **unchanged**, which is
the right direction: the four new revisions are all post-306 and stale.

The replay 323 **refused** reproduces too, and that is the load-bearing half —
the finding is that the obvious instrument says "refuse this":

```
python3 <asof.py>
#  wake-dates in log: 26
#  {'ok': 13, 'STALE': 13}          zero SKEW
```

`git rev-list --count 152b9bc9` → **2,004** ✓.

---

## Slice 309 — 16 of 17, and the one that fails is `327.1`

Reproduced: `1f75dab4`'s diff verbatim (all three calls swallowed by the
trailing comment); `grep -rc 'bo-alert__dismiss' examples/po-app/` → **0**;
exactly **two** routes carry a `.bo-data-table__select-all` (`server.mjs:458`
`/pos`, `:1462` `/stress`); `npm run check:po-app -w docs` → **20 behaviours**;
`apps/docs/scripts/measure-stress.mjs` + `po-app-harness.mjs` committed and
`measure:stress` wired; `/components/data-table` carries the withdrawal, not the
numbers; `roadmap_scope.py --self-test` green **with case F**; and 309.4's
metric table:

```
python3 -  # over .roundtable/loop-metrics.jsonl
#  total samples: 132   distinct names: 44
#  ci-wall-time    26 samples,  1 day, newest 2026-08-18 19:54   span 02:48 -> 19:54 (17h 06m)
#  axe-violations  17 samples,  8 days, newest 2026-09-06
#  bundle-gz-kb    11 samples,  5 days
#  claims          11 samples,  3 days, newest 2026-09-06
```

**The failure — `327.1`.** *"`/movements`, `/inbox` and `/receive` were
unaffected: each runs its own inline `initDataTables()`."*

```
grep -n 'initDataTables()' examples/po-app/server.mjs
#  158   the shared page() template  (the broken line, since fixed)
#  1006  movementsScreen             the ONLY inline call in the file
```

Counted inside each screen's own source span
(`movementsScreen` 973-1092, `receiveScreen` 1093-1205, `inboxScreen` 1206-1352):

| screen | inline `initDataTables` | `.bo-data-table-container` | `select-all` | `row-select` |
|---|---|---|---|---|
| `/movements` | **1** | 2 | 0 | 0 |
| `/receive` | **0** | 1 | 0 | 0 |
| `/inbox` | **0** | 1 | 0 | 0 |

`initDataTables` binds `.bo-data-table-container`
(`packages/core/src/js/behaviors/data-table.ts:98-101`), so `/receive` and
`/inbox` were **unbound for the same 15 days** — the opposite of the published
reason. They were unaffected regardless: `bindContainer` only listens for
`change` on row-selects and the select-all, and both screens carry **zero** of
each, exactly as `initAlerts` was inert one row above in the same table.

The correction is in Slice 309's own text (236.2 permits amending archived text
for a correction).

---

## What the two defects have in common, and the item filed for it

Both are unmeasured sentences shipped under the credibility of a measurement
taken properly in the same slice. 309's blast-radius table was probed live over
all 10 GET routes; the sentence next to it was read off the code. 326's control
table was measured with the negative controls no earlier record carried; the
revision it was measured at was not written down, and the record then changed
the thing it measured.

CLAUDE.md already carries the rule that would have caught both — *"list the
other claims a change is carrying and name the instrument for each; 'none — I
read it off the code' is the answer that predicts the defect"* (192.1). It did
not fire here, and it did not fire in the three previous grills (`322`, `319`,
`291`) whose defects have the same shape. **That is filed as `327.3`**, with its
Accept written so that *"the rule is fine, the practice is the gap, build
nothing"* is a satisfying outcome — and with a gate refused in advance on
94.11's rule, because *"this claim names its instrument"* is semantic.

**Refused, recorded so it is not re-proposed:** a new `CLAUDE.md` section for
the self-destroying-control shape. That file already states the mirror image
(*"an assertion that can be tripped by its own explanation is a detector that
cannot pass"*), and `326.3` is open on the loop's prose growing because rules
keep being added. One pinned revision plus one sentence in the lane that uses it
is the smaller fix.

## This grill's own errors, both caught before they landed

- **It first wrote that trap 1 had NOT bitten this container**, on the strength
  of `git rev-parse HEAD origin/main` agreeing at Step 0. That is exactly the
  false comfort `ENVIRONMENT.md` §1 names: `git branch --show-current` was
  **empty** and local `main` was stale at the pre-rebase `26447ba9`. Caught by
  running the one command that answers it, inside a grill about unmeasured
  sentences. Fixed with `git checkout -B main origin/main` before any commit,
  and the wrong claim is recorded in Slice 327's trace rather than quietly
  replaced.
- **The identical `was` columns in Slice 308's and Slice 326's region tables
  read as the identical-value-across-inputs tell** and were nearly filed as one.
  `git log -1 8848ed55` settles it: the two tables share a base commit. A
  suspicion is not a finding until the cheaper explanation has been excluded.

## Not verified, said plainly

**No screenshots — a cloud wake has no Podman and no `localhost:8081`.** This
grill's diff is three markdown files (`ROADMAP.md`, `LOOPS.md`, this report) and
touches no CSS, no page source and no built layout, so there is nothing in it a
screenshot could check. That is a statement about this diff, not a claim that
screenshots were unnecessary in general.

The visual debts `RESUME.md` carries forward are unchanged and unspent — this
wake added none.
