# Open owner decisions

1. O1 (before the bootstrap). Who dispatches. Choose one: (A) the local /loop only, with every cloud session archived (recommended for this milestone: simplest, and it removes collisions 6-8); (B) local /loop as the only committer, with cloud wakes as read-only auditors filing GitHub issues (not yet verified that the cloud token can file issues); or (C) keep 'accept collisions'. Either way, will you archive every active session of trig_019aw8t…, across every page of list_runs?
2. O2 (before the bootstrap). For each uncommitted path, commit to main, move to a branch, or discard: the 09-20 erp-suite checkpoint (examples/erp-suite/*, journey/, check-journey.mjs, suite.json, gen-suite-index.mjs, screen-kit.astro, erp-suite-gaps.md); scripts/loops/review_depth_gate.*; the three untracked .roundtable/grill-* files; .claude/skills/hallmark; .agents/; skills-lock.json; the modified .gitignore and package.json. Moving the journey off main closes 389.6 and 389.7 as superseded.
3. O3 (before the bootstrap). Approve the one-time bootstrap GOAL that runs 393.1-393.10 in their After: order and stops. Should rules 2 and 3 pause until 393.10? Recommended: yes, because M0 changes the counters they read. What is the cap on M0 wakes?
4. O4 (before ACTIVE; a one-way door). Adopt the drafted replacement for CLAUDE.md:337-343? Every published name names a shape. Module words live only in examples/erp-suite demo data, the generated job index and the consumer. The swap test decides job words. A layout a module needs becomes a new shape with @status experimental. No @busy-office/modules or blueprints package for now; revisit if an outside consumer asks to install module screens. Recommended: yes. Reversing later costs what the invoice-list rename did: 162 references in 94 files, 34 days on.
5. O5 (before ACTIVE; closes 377.6). Define the app: name the long-use app, and say whether busy-office-erp is its first user. Close the module list: Home/role, Profile, Configuration, Finance, Sales, Distribution, Inventory, Procurement, Production Planning, plus any 'etc.' module such as HR, Quality, Maintenance, Service or Projects. Place AP and AR (under Finance, or under Procurement and Sales?). Choose the demo industry (discrete manufacturing matches the existing BOM). State whether rugged RF devices are in scope. If yes, 17 Slice-389 findings become acceptance tests for 396.12; if no, they return to the backlog.
6. O6 (before the first experimental part merges; closes 377.5). Release the unreleased stable fixes (322 commits since v0.8.0 as of today), or record why not, so that no release mixes them with unlabelled experimental surface?
7. O7 (before 394.4). What is the tier word? Recommended: 'experimental' (0 existing uses in source or docs; the word Node and Primer use). Your 'preview' already has 123 other meanings, including the docs container in 377.12. Your 'init' collides with 26 init*() functions in llms.txt.
8. O8 (before 394.4). Experimental parameters. (a) Ship experimental parts in the npm tarball at their final path, css/components/<name>, left out of index.css, marked at every use, with no Breaking entries (recommended), or keep them repo-only until an external consumer exists? (b) WIP cap: 2 (recommended; no new component directories in 33 days), 3, or another number? (c) What makes two uses independent: distinct (job, pattern) pairs among worked screens, where a module label never counts (recommended)? (d) Entry bar: one real use plus a named, queued second use on a different pattern (recommended), or two independent needs before any code? (e) Who graduates a part: the loop's N.3 Objective grill, with you seeing the 'Added' entry at release (recommended), or your explicit approval of each part? (f) May an experimental part exceed the browser floor while it is outside index.css?
9. O9 (before 394.10). Approve the new public surface in one minor release with Added entries: exports ./patterns, ./jobs and ./llms.txt with schema tags, and the attributes data-bo-pattern, data-bo-gap and data-bo-experimental. Publishing stays owner-triggered.
10. O10 (decide before 396.5). 112.3 and 112.4. Choose one: (a) lift 112.4's dependence on the 112.3 pilot and re-scope it as a pattern-keyed Screen Contract whose regions start as warnings, consolidating score.mjs OWES and patterns.json States, sequenced after 394.10, with 112.3 kept as the job index's evaluation (the roundtable's recommendation); or (b) keep both blocked as they stand.
11. O11 (before ACTIVE). What takes precedence while M1 is ACTIVE? Options: interleave one dispatch in 3 with the 15-item defect track (recommended); preempt everything except P0; or run after a named item.
12. O12 (before ACTIVE). What do rules 2 and 3 do during the milestone? Options: scoped to M1 rows and graded against the Done-test (recommended); normal; or suspended, with one sweep and one grill at close. Decide after 393.5 shows 381.2's base-rate count.
13. O13 (before 394.14). Approve a third declared Jev point, the queue screen JQ, by amending CLAUDE.md:88-100 (currently 'exactly two points' and 'not a gate'). JQ asks three fact questions about an item the code has already picked, never picks or approves anything, runs in shadow, and only you can promote it. What may be sent to jev-ai.pro? Recommended: roadmap item text only. The repo is PUBLIC, so that text is already public. No customer or business data.
14. O14 (before ACTIVE). Models. Which tier is the planner, your 'bigger target'? The jev-triage design names Claude Fable 5 or Opus 5.5 at effort xhigh; this redraft did not verify names or prices. May the balanced and fast tiers take the mechanical and collect routes? If not, every route maps to top, and a route only chooses skills and critic. May the planner ever be a non-Claude model? Recommended: no, because it would partly reverse Slice 375.
15. O15 (before 394.13). For queue-screen calibration, should re-plans that measurement discovered after work started count as 'execute'? These are about half of historical re-plans (7 of 14 in the pilot), and nothing in the item text shows them. Recommended: yes. If they count as misses, no screen can pass the zero-miss bar, and the screen stays code-only.
16. O16 (before 393.7). Direction triggers. Should D1 (nothing dispatchable) run one bounded planner review, at most once per 24 hours, before rule 6 Polish (recommended), or only where rule 8 would stop the loop today? Should D4 (purpose drift) be on? If so, set N landings and the X% framework-path share. Product was about 11% of changed lines since 09-18, excluding roadmap sweeps.
17. O17 (before ACTIVE). Budgets: total milestone wakes; agents per wake (the measured hardware ceiling is 8); wall-time cap per Workflow (completed workflows ran 35 and 60 minutes, the uncapped one 2h52m or more); the RESUME.md line cap (suggested 120); and new items per direction review (suggested 5).
18. O18 (before first use). Confirm four things. (a) 'Freedom to design' does not reopen refused-on-record items (Gantt, map, drag, dock, grid engine, charting engine and the others). Each needs an owner entry naming new evidence. (b) Screens with placeholders are published in the Screen kit with a visible 'Placeholder' badge (recommended), or held back. (c) Confirm the 9 parked items, and say whether you want 381.1 unwired now. (d) Domain briefs are AI-researched with at least 2 independent sources per claim and reviewed by you. NotebookLM notebook 89417194 is a lead only. Resolve GAP-21 by queuing a where-used second use, or by closing it as refused.

# Critic: what it fixed

## Missing
- Loop-log rows carry no milestone or route tags. The redraft adds `--milestone`, `--track`, `--route`, `--model`, `--agent`, `--skill`, `--first-try` and `--screen`, but never says they are written into the loop-log.md row. `dispatch_status.py` reads the log (`ROW`, dispatch_status.py:39), not loops.db. Without the tags in the row, `Rules-2-3: scoped` and the interleave count cannot be computed. The storage doctrine also requires `rebuild_from_log.py` to restore every new column. Old rows cannot be backfilled: `record_iteration.py` forbids it, and LOOPS.md records it as refused. Fixed in 393.5, 393.6 and prompt §11.
- Folded (absorbed) items had no `Route:` and no `After:` line on the item that absorbs them. 393.4 refuses to print a verdict while any ACTIVE-milestone item lacks a Route. Rule M takes the oldest item, so it would dispatch 377.x, 389.x and 249.7 alone, before any 393-397 item. Fixed in the realignment table and 393.9: every absorbed item gets Route and `After: <absorbing item>`, and closes in that item's commit. 377.4 has no absorbing item, so it moves to the defect track. `classify_r2.py` now gives 3 / 22 / 16 / 9 / 15 over 65, and fails on an injected drop or duplicate.
- No owner item existed for the module-form choice after the A/B test. 396.6-396.10 waited only on 396.5, which just files the question. The redraft's own order simulates as 396.5 → 396.6 with no owner step between. Added 396.13 (OWNER, reserved), and 396.6-396.11 now wait on it.
- No item existed for O13. CLAUDE.md:88-100 says Jev is used at 'exactly two points' and 'is not a gate', so 394.14 could be dispatched before that text is amended. Added 394.18 (OWNER), and 394.14 waits on it. O13 no longer blocks ACTIVE.
- Owner answer 5 ('if we don't consider new, we could miss as well') was not implemented. The redraft closed single-use needs as REFUSED, and stage 7 deduplicated against SEEN, which includes REFUSED. A later second use was therefore thrown away. Now every hit on a known key is added as a use (`Use:` lines). A single-use need stays OPEN with `Waiting: second use`, and code re-files it for triage when it reaches 2 distinct (job, pattern) pairs. REFUSED is kept for refusals on the merits (prompt §8 stage 7, §9.3; 394.8; Milestone bet 3 and Done-test 4).
- Experimental JavaScript behaviours and tokens were not covered. `src/js/index.ts` re-exports every behaviour, and versioning.astro:15-17 says exported `init*` signatures are API. Added: an experimental behaviour ships only at the per-file `./js/behaviors/<name>` export and is kept out of the index, and an experimental part adds no `--bo-*` semantic token (394.4, §9.3).
- `derive-introduced` would record an experimental part's first tarball as 'introduced', because the final-path choice ships `dist/css/components/<name>.css`. A graduated part would then look stable since its experimental release. Added a stable-since version to the maturity record (394.4, §9.3).
- Some readers of api.json fail closed. `ClassRef` and `ApiTable` (required by page-shape), `check-sample-classes` and `check-markup` would render an empty table or fail on an experimental page. 394.4 now commits the list of readers and how each treats an experimental part.
- The docs build runs `check-markup.mjs dist` (apps/docs/package.json `build`) over `dist/suite/`, which `copy-suite.mjs` fills. The first placeholder would therefore fail the docs build, and the redraft only gave `suite:check` its `--allow-gaps`. Fixed in 394.5, §9.2 and stage 5, with a red-proof.
- 394.10's build order. patterns.json, jobs.json and llms.txt are generated by the docs build, but publish.yml builds core (ending in `check:package`), then docs, then publishes. Added the property: a core-only build produces the three files, and `check:package` resolves every export.
- llms.txt is the 112.3 pilot's instrument. ROADMAP.md:3002 says any edit to it is written into 112.3's record. 394.6 and 394.10 now do that, and still write nothing into `.roundtable/pilot-112/`.
- N.1, N.2, N.3 and fill items had no `Route:`, and the item lint rejects any item without one. Added a routes table in prompt §5, updated §8 stage 9 and E-items, and made N.3 wait on 377.4 when the part has a pointer listener.
- ENVIRONMENT.md was dropped from the read set. LOOPS.md Step 0 requires it, and `check-resume-charter` asserts that RESUME.md points to it (prompt §4, 393.8).
- 393.10 did not say who re-scores or what is re-checked. It now requires a fresh-context scorer, a re-check of each of the four Invalids named on 09-25 by its own command, and 393.1-393.9's closes re-run on a clean HEAD (377.3: 2 of 30 closes were false at HEAD).
- The icon budget (M9) was claimed as carried into 395.1 but is not in its Accept. Added; an icon set is refused on record (scope.astro).
- 394.2 left several decisions out. It did not map the existing modules `o2c`, `p2p`, `crm` and `journey` (_shell.mjs:42-49) to the owner's list, which has no CRM. It did not decide whether Profile and Configuration are rail modules or menu destinations (round-1 lens 3), or where the frame's Settings screen ends and 396.3 begins. It did not separate facet words from directory ids, or say how a layout reaches the long-use app. Added, together with `App`, `Modules` and `Devices` fields in the Milestone block.
- jobs.json is a published export, but its compatibility class was unstated. Added: the schema is API; row values (jobs, also-called words, facets) are data that may change in any minor (394.10).
- The queue screen (JQ) did not cite the reopen conditions in grill-kev-in-the-loop-2026-09-21.md (outcome labels; answers stable under unrelated prose). It did not add JQ to jev-rubrics.md, and it did not say that Rubric 3 stays off. Added to §6 and 394.14.
- `check-experimental` reads ROADMAP.md, which the docs Containerfile does not copy, and the redraft did not say where it runs. It now runs on the loop path and in CI, and stands down loudly anywhere else (394.7).
- Specs and reconcilers were cited by scratch paths under /private/tmp: design-preview/marker_check.mjs, design-jev-triage/queue_screen.gate.json and redraft/*.py. P5 now commits them to `.roundtable/milestone-m1-2026-09-25/`, and the roadmap cites those copies.
- §9.1 omitted two refusals from loop-log.md:939: 'number abbreviation' and 'group-by-with-counts'.
- A long-term naming trap: a part is born with its final name when it has only one use. Added to G2: the swap test is re-run with both uses in hand, and a rename is free while the part is experimental.
- Smaller gaps, now fixed: the interleave had no fallback when no defect item is dispatchable. The bootstrap GOAL did not say that a P0 still preempts. P2's path-list check missed the untracked `.agents/`, `skills-lock.json`, `.claude/skills/hallmark` and the three grill files. The realignment commit was not recorded with `record_iteration`. 394.7's browser gate needs `serveDist`. 396.1 must be verified in the docs container build.
- Owner answer 1 is not live until promotion. In shadow, 'Jev needs to call another model' only becomes true at 394.16, which needs at least 20 joined outcomes. Added an owner-chosen escalate-only mode (394.18, §6): a `sharpen` answer launches the planner, which is not told why it was sent, at most once per item. Every other answer is logged only.

## Wrong
- The ordering claim is wrong. The redraft says order is enforced by `After:` lines and that 'component work (Phase 3) has to interleave with layouts'. `simulate_rule_m.py` (critic2 scratch; fails on an injected `After:` cycle) shows otherwise, because rule M takes the oldest dispatchable item. The frame (395.1) is dispatch #26, behind all 12 M0 items and 13 Phase 1 build items, including the queue screen 394.12-394.15. Slice 397 items run only after every dispatchable Slice 396 item. Nothing interleaves. The text is corrected in §4, the Slice 394 and 397 headers and the Milestone section, and the lever for a sooner frame is stated.
- 397.1 had the wrong dependencies. Its `After:` included 394.15 (the queue screen), which it does not depend on, and omitted 394.17 (the incubation recipe), which it should follow. Its Accept also required the second composition (N.2) and the decision (N.3). If 396.5 waits on 397.1, as it must for the lifecycle to be proven before the five-module fan-out, N.2 could sit in a module that waits on 396.5, and the two would deadlock. Fixed: 397.1 runs as far as the fill, and 396.5 waits on 397.1.
- 397.2's prose says 'the OWNER items are excluded from After:', but 394.1-394.3 are listed there. 394.17 was left out with no reason given. Module items that 396.11 files later cannot be listed at all, so `milestone.py` now holds 397.2 while any other Phase 2 item is open.
- §12 listed 'the experimental cap is reached' as a stop. That halts the whole loop on a normal state. It is not in the Milestone `Stop` field, and the entry test's cap check (E-cap) already refuses entry. Changed so that the need stays OPEN behind its placeholder and the loop continues.
- The stop 'two consecutive wakes make no M1 progress' fires on in-flight holds (§4) and on wakes that rule 4 restricted to the defect track because every M1 item is blocked (§4). Both contradict the redraft's own rules. It is now defined as two dispatch wakes on the same item, neither ending `landed`.
- The one-way-door stop for 'a public API change outside the experimental tier' would halt 394.10, whose exports O9 approves in advance. Surfaces that an answered owner decision approved are now exempt.
- §8 said each module item 'runs these stages as one Workflow'. That is impossible: stage 5 writes the registries and builds, which only the dispatcher may do, and the critics need the built output. Rewritten as Workflow runs alternating with dispatcher steps, each run with its own in-flight line.
- J1 at §8 stages 3 and 8 ran in the middle of a Workflow, which contradicts §6 ('Never inside a Workflow script'). Moved to the next dispatcher step. No J1 number decides anything, so nothing is lost.
- 393.5 took the closed set of loop names from 'the set LOOPS.md names'. The log contains Meta (577 rows) and Gauntlet (1), and LOOPS.md's table has no Meta, so recording a refusal row would fail. The set now comes from the log plus the table, and `check-loop-vocab` is extended to it.
- 396.1 took module terms from 394.2, which lives in ROADMAP.md. The docs Containerfile does not copy ROADMAP.md and records two gates that broke the image that way, and the gate outlives the milestone. It now reads `_shell.mjs` MODULES, which 394.9 fills from the Milestone field.
- 'The common Accept for 396.3-396.10' included 396.5, the A/B run, which is not a module item.
- The Milestone section had two errors. 'What it produces' put the Jev queue screen in Slice 393; it is 394.12-394.16. Done-test 2 said every module has its layouts built, which contradicts 396.5's option (b), job rows plus new-shape screens only.
- §11's record command omitted `--agent` and `--skill`, which 393.6 adds.
- The redraft's `lint_roadmap.py --red-route` injection is not anchored to an item line. On the patched text it replaced a prose mention of 'Route: design' and came back green. Every injection now matches one anchored item line and fails loudly on a miss.
- Grill report (not returned): 'Seat D is the spine' overstates. D's central artefact, the Screen Contract (112.4), waits on O10. What was adopted is seat A's rule plus D's job index.
- The Slice 394 header said 'every Phase 2 item carries `After: 394.1, 394.2`'. 396.2 and 396.4-396.10 inherit that dependency only through their `After:` chains.

## Ambiguous
- '(Which is bigger target)' can mean the bigger model (the planner on the top tier) or the bigger goal (setting direction matters more than dispatching single items). The plan serves both readings; confirm which in O14.
- 'The long use app' could be `examples/erp-suite` (the reference app, outside semver) or busy-office-erp. How a layout reaches the real app is undefined beyond O5's single conformance property; 394.2 now asks.
- Domain packs: loop-log.md:595 and 95.2 ('composable, do not build') conflict with owner answer 3 and with 396.5's 'full module sets' option. The redraft listed domain packs as refused in §9.1 while also planning module sets. The choice now depends on 394.1's ADR superseding or upholding :595 for `examples/erp-suite`.
- The O-list (not returned) now has stale references. O13 says 'before 394.14'; it is now 394.18. O9 should name the new `api.experimental` key in api.json. O5 should cover the o2c/p2p/crm/journey mapping and where Profile and Configuration go. O11 should state the machinery-first order: the frame lands at dispatch #26.
- The boundary between the frame's Settings screen (395.1) and the Configuration module (396.3) is undefined until 394.2 draws it.
- Escalate-only makes Jev's answer change what happens to an item. Whether that is 'a gate' under CLAUDE.md's 'not a gate' is settled only by the owner's amendment at 394.18.
- The Precedence, Rules-2-3, Dispatcher, Planner and Direction-drift fields ship pre-filled with recommendations rather than OWNER. `milestone.py` cannot tell whether the owner confirmed them; the O-table is the only record.
- Order preference: machinery first (frame at dispatch #26, then the queue screen screens every layout) or frame first. The draft keeps machinery first, with reasons, and names the lever: `After: 395.1` on 394.12, 394.13 and 394.17. `Precedence` cannot reorder items inside M1.
