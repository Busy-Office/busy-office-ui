# Objective grill — Slices 276, 277, 278, 279 (2026-09-05, cloud wake)

Dispatched by rule 3 (`Objective 5 / 3 slices … OVERDUE [274, 276, 277, 278,
279]`) — the first dispatch under `279.4`'s correction to `CLOSES_A_SLICE`.
Roadmap slice: **280**.

## Scope — one of the five armed slices is dropped, and that is the point of step 0

`LOOPS.md` §6 step 0 exists because the armed set names slices *touched since
the last Objective row*, so a slice with rounds either side of a grill re-arms:

```
python3 scripts/loops/dispatch_status.py
grep -hoE '^## Slice [0-9]+ — Objective grill of Slices [0-9, -]+' \
    ROADMAP.md ROADMAP-archive.md
```

The most recent grill is **Slice 275, covering 271, 272, 273 and 274** — so
`274` in the armed set is exactly that re-arm and is **dropped, grilled in full
one wake ago**. No grill in either file names 276, 277, 278 or 279.
**Honest scope: 276, 277, 278, 279.** `.roundtable/INDEX.md` reports **4
repeated subjects** (`object-page`, `external-review`, `site-proposal`,
`unit-doctrine`), none of them this set.

## Method

Every figure the four slices state was re-derived from git, the built `dist`,
the shipped manifests or a live gate run — never read back from the prose that
stated it. Where a claim describes a *commit*, it was re-measured **at that
commit** (`git show <sha>:<path>`), which is the distinction Slice 275's own
grill turned on and which one figure here needed.

## The score: 52 of 54, and both failures are one table

| slice | checked | hold | fail |
|---|---|---|---|
| 276 | 15 | 13 | **2** |
| 277 | 13 | 13 | 0 |
| 278 | 11 | 11 | 0 |
| 279 | 15 | 15 | 0 |

## The defect — 276.1's blind-commit table measured the tree its own fix had not reached

276.1 widened a Polish surface's source set to include the behavior modules
that drive it, and justified it with a table:

> **31 blind commits across 7 surfaces**: `data-table` 19/30, `scan` 5/6,
> `pagination` 3/4, `stepper` 2/3, `tree-table` 1/3, `alerts` 1/4,
> `dashboard` 0/2. The other fourteen ledger surfaces have no serving module.

Re-derived at `29a9062b` — 276.1's own commit — from `polish_requeue.py`'s own
source map:

| surface | blind / commits |
|---|---|
| data-table | 19 / 30 |
| **inline-editing** | **10 / 11** |
| **table-toolbar** | **10 / 10** |
| scan | 5 / 6 |
| pagination | 3 / 4 |
| stepper | 2 / 3 |
| tree-table | 1 / 3 |
| alerts | 1 / 4 |
| dashboard | 0 / 2 |

**51 blind commits across 9 surfaces; twelve ledger surfaces have no serving
module, not fourteen.** All seven published rows reproduce exactly — the
defect is the two that are missing.

### Why it is a defect and not a scoping choice

The two dropped rows are **precisely the two `PAGE_ONLY_BEHAVIORS` surfaces
276.1 itself added**. So:

- the summary sentence *"the other fourteen ledger surfaces have no serving
  module"* is only true if those two have none — which denies the map named in
  the same item's Accept, forty lines further down the same docstring;
- and the dropped `inline-editing` row is the **10 of 11** that 276.1's *own
  opening paragraph* quotes, calling it *"the sharpest case"*. One number in
  the item contradicts another number in the item.

The general shape, which is the reusable half: **a measurement taken to justify
a change must be taken under the change, or say which side of it it is on.**
276.1 measured "how blind is the source map" with the OLD source map and
published it as a property of the tree. That is Slice 275's family — a figure
the wake's own commit moved — one level up: not a figure the commit moved, but
a *set* the commit widened.

### The instrument was wrong first, and the tell was a plain zero

A probe written against `polish_requeue.py`'s own functions reported **0
surfaces with a serving module** — a zero across every input, which CLAUDE.md
says is a defect until proven otherwise, and was: `pr.rows()` yields
`(line_no, surface, rest)`, not `(kind, name, rest)`, so every surface string
came out as `97/component/alerts` and every lookup missed. Corrected, it
reproduces all seven published rows *and* the item's separately-stated
`inline-editing` 10/11 — two reconciliations against figures it was not fitted
to.

Then hand-checked rather than left to the probe:

```
git log --format=%h 29a9062b -- packages/core/src/js/behaviors/row-edit.ts | wc -l
#  11 — one (9d1ecbe2) also touched inline-editing.astro   -> 10 blind
git log --format=%h 29a9062b -- packages/core/src/js/behaviors/table-toolbar.ts \
                                packages/core/src/js/behaviors/data-grid.ts | wc -l
#  10 — none touched table-toolbar.astro                   -> 10 blind
```

A second, independently written compression of the same walk (now published
beside the corrected table in ROADMAP 276.1) returns `total blind: 51` and the
same nine rows. **It was run before being published**, because a command
printed next to a claim that does not execute is worse than no command.

### Where the number had spread — four places, three corrected

| file | action |
|---|---|
| `ROADMAP.md` 276.1 | corrected, with the re-runnable probe added beside it |
| `.roundtable/polish-state.md` (charter paragraph) | corrected |
| `.roundtable/polish-state.md` (detail table) | two rows added, total corrected |
| `scripts/loops/polish_requeue.py` docstring | corrected — **the worst of the four** |
| `.roundtable/loop-log.md` row for 276.1 | **left alone**, deliberately |

The docstring was worst because it states the seven-row table forty lines above
the paragraph explaining that the two page-only surfaces *are* reachable, so
the file contradicted itself — the same class Slice 265's grill filed as *a
gate header still encoding the declaration its own slice corrected*, recurring
in a script header. The log row stays because `record_iteration.py`'s standing
rule forbids backfilling rows, and a log row records what was said at the time.

### Refused, measured

Turning the blind-commit walk into a gate, or a `--report` flag on
`polish_requeue.py`. The predicate *"a commit touched a serving module and none
of the surface's own paths"* is **51 of 73 qualifying commits at `29a9062b`** —
the normal state of the repository, not a defect signal, so a gate over it
would be red on a correct tree. That is 94.11's base-rate refusal exactly. The
number is a one-off justification for a shipped change; what it needed was its
command written beside it, and that is what landed.

## What held, and how it was checked

Recorded because a grill that lists only defects hides how much of the record
is sound.

**279 — every replay figure reproduces to the character.** At `632bfc4`, the
log parses **1,437 rows against 1,437 raw `^- ` bullets**; `Polish` names a
slice on **18 rows / 17 slices**, `Meta` **3 / 3**, `Optimize` **0**; the
twelve slices named by no Continue/Standardize row come back as the identical
list `182, 217, 220, 242, 266, 267, 268, 269, 270, 276, 277, 279`; whole-log
crossings **51 → 52**.

*A modelling note, since it cost a round:* a first replay returned **76 → 80**,
because it reset the distinct-slice set every time it reached 3. The counter
resets **only at an Objective row** (`since_last`), so a window crosses at most
once. Modelled that way — windows reaching 3, and first-crossing-per-window,
which agree — it is 51 → 52. The published figure was right and the first
instrument was not.

**279.1 / 279.2.** `grep -c -F 'components/scan'` over the five RF sources reads
`0/0/3/3/3` at `4c3d635` and `2/0/3/3/3` now. The new gate arm reports *"39
pattern(s) list only components they render, and 4 screen(s) rendering an
attribute-only component (scan) all name it"* — an independent reconciliation
of the corrected `fit` cite's own numbers. `check-components-used --self-test`
passes **11 cases** including the `data-scan-inputs` prefix trap;
`dispatch_status.py --self-test` **14**.

**278 — the figures survive the trap that killed three of Slice 275's.**
`grep -c -F data-col-toggle apps/docs/scripts/check-claims.mjs` reads **2**
today but **1** at `aa7d433d`, the commit that published "0 → 1"; the second
line arrived with `a098cf85` (278.5) an hour later. Read at the commit, the
figure is right. Same for `Home` in `data-grid.test.ts` (**0** at `7f4792e7^`,
**3** now) and `initDropdowns` pages (**9** at `a098cf85^`, **10** now).
`data-grid.ts` defers with `setTimeout(…, 0)` and **not** `queueMicrotask`, at
line 176.

**277 — six cites hold.** `pagination.css` has `font-size` **twice, both
`var()`**, **0** hex, **0** unit-bearing literals; the `aria-current="page"`
contract comment is at **lines 5-6**; `[aria-current="page"]` sets
`font-weight` as well as colour. `load-more-auto.test.ts` carries **6** cases,
`load-more.test.ts` keeps its no-IntersectionObserver case, and the corrected
wording sits on all five sites (`windowed-list.ts:255`, `load-more.ts`, three
in `pagination.astro`). The two surviving `scrolls into view` hits are both the
*explanation of the removal* — CLAUDE.md's "assert on structure, never on raw
text" rule working as designed.

**276 — everything except the table.** `--check` now reports behavior modules
in the source sets (`wizard.ts`, `data-grid.ts`, `tree-table.ts`);
`PAGE_ONLY_BEHAVIORS` carries exactly two entries, each reconciled against the
page's own import on every run; 276's commit touches **0** files under
`packages/core/src/` and **0** docs pages.

## Gates

`build -w @busy-office/ui`, `test -w @busy-office/ui` (29 files, 165 tests),
`lint:css`, `docs:build`, `check:repo` (re-run after the source edits — 818
slice-ref assertions, 262 slice numbers), `check:formatting`, `check:claims`
(**167** live; the *3 NOT VERIFIED* is `ENVIRONMENT.md` 6b, this container
reporting `(hover: hover) and (pointer: fine)` false), `test:axe` (127 pages x
2 widths, zero violations), `check:layout`, `check:scroll`,
`check:wrong-choice` (156 assertions, 1 outstanding).

## Not verified

**Nothing in this diff renders.** `ROADMAP.md`, `.roundtable/polish-state.md`,
this file and `scripts/loops/polish_requeue.py` — **0** files under
`packages/core/src/`, **0** docs pages, **0** CSS. The 1440/390 light-and-dark
screenshot lane a cloud wake cannot run therefore has **no subject here**. That
is an absence of subject, not an unverified claim, and it is stated rather than
left for the reader to assume either way.
